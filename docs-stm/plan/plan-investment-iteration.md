# 投资功能优化 — 迭代实施计划（20 轮，每轮量化验收）

> 关联：设计 [`plan-investment-features.md`](./plan-investment-features.md)（功能设计 + 章节归并方案 §4）· 顶层章节机制 `report_section_order`
>
> **定位**：本文档是**实施计划**，把「章节归并方案」落地为 20 轮可量化验收的迭代。核心约束：**21 章总数减至 19**（合并持仓重合度/相关性矩阵为持仓关系矩阵、合并组合历史走势/历史回撤为组合历史走势与回撤、合并基金风格/因子暴露为风格与因子分析 -3、新增行动建议独立章 +1），其余功能作为现有章节的子模块/子块/子列融入。每轮有**量化验收标准**，达标才进入下一轮。
>
> 前置：`plan-investment-features.md` §4 已定章节归并对照表。本文档按 §5 实施次序拆分为 20 轮。

---

## 1. 迭代总览

**总目标**：21 章总数减至 19（合并「持仓重合度矩阵」+「持仓相关性矩阵」为「持仓关系矩阵」、合并「组合历史走势」+「历史回撤分析」为「组合历史走势与回撤」、合并「基金风格分析」+「因子暴露分析」为「风格与因子分析」-3、新增「行动建议」独立章 +1），功能从「事后体检」扩展至「买入比较 + 决策行动 + 数据可信」，用户感知章节更精简、单章更充实、决策更直接。

**阶段地图（20 轮 / 7 阶段）**：

| 阶段 | 轮次 | 主题 | 承载章节 |
|:--|:--:|:--|:--|
| **A 数据质量地基** | 1~3 | 数据质量仪表盘 | 改造 18 章 |
| **B 决策中枢** | 4~7 | 调仓建议/交易纪律/收益归因 行动板块 | 新增 17 行动建议（always）+ 12 章摘要引用 |
| **C 历史增强** | 8~10 | 危机区间标注/尾部风险/快照差异 历史与演进增强 | 改造 15 组合历史走势与回撤 / 16 组合演进 |
| **D 风格与选基** | 11~12 | 合并风格与因子 + 行业 Beta + 候选基金比较 | 改造 9/5 章 |
| **E 成本流水** | 13~15 | 资金流水 + 资金加权收益 | 输入扩展 + 改造 1/2/3 章 |
| **F 估值与温度** | 16~17 | 估值分位 + 市场温度 | 改造 4/1 章 |
| **G 导航与收尾** | 18~20 | 分组标签 + 文档 + 全链回归 | 全报告 + 发布门禁 |

**迭代原则**：
1. **每轮独立可交付**：每轮结束产物可运行、可测试、可回滚；
2. **每轮有量化验收标准**：测试数 / 覆盖率 / 计算精度 / 行为断言 / 向后兼容，达标才进下一轮；
3. **默认关**：所有新子模块 config 开关（`report_submodules.*`）默认关闭；「行动建议」顶层章节开关 `enable_action` 默认关——不开时报告为合并后 18 章基线，开启才出现第 19 章；
4. **不破坏既有**：旧持仓文件格式、旧 config、既有章节输出**向后兼容**（回归测试保证）。

---

## 2. 通用验收标准（每轮强制，不逐轮重复列出）

以下标准**每轮必过**，轮 1~20 的"量化验收"是对此的**叠加**：

- **[门禁-P0]** `python scripts/test_runner.py --mode dev-verify` 全部通过（核心单元 + 基础场景）；`python scripts/check-code-traces.py --ci`、`python scripts/check-doc-traces.py --ci`、`python scripts/check-task-numbering.py --ci` 三脚本通过。
- **[测试规范]** 新增/修改测试用例**必须标注对应 pytest marker**（`unit_*` 子标记，新 marker 注册到 `conftest.py` 并同步维护文档）；LLM 相关测试一律 mock（禁真实调用）。
- **[隔离]** 遵守 `_isolate_sensitive_paths` / 单例重置 / output_dir 重定向 / 持久化文件路径隔离；新持久化文件须加入 fixture。
- **[覆盖率]** 涉及模块整体覆盖率 ≥80%；**新增/改造模块 ≥85%**。
- **[向后兼容]** 旧持仓文件（无流水页签）、旧 config 解析与改造前**完全一致**（回归断言）；既有章节输出不因新子模块开关默认关而改变；`enable_action` 默认关时报告为合并后 18 章基线（无行动建议章）。
- **[回填]** 每轮完成：`docs-stm/managements/changelog.md` 记一轮；自审问题入 `review-findings.md`（修复后即归档）。
- **[架构]** 不违反 C1~C20（尤其 C3 原子写、C8 日志统一、C11/C12/C13/C14 测试规范、C16 路径绝对化、C18 凭据隔离）；管线共享层 `report/orchestrator.py` 不因 Web 无关改动而破坏。

---

## 3. 二十轮迭代计划

> 每轮"量化验收"为**叠加标准**，与 §2 通用标准共同构成达标条件。验收用「构造→断言」描述，均可自动化或半自动化核对。

### 阶段 A 数据质量地基（轮 1~3，改造 18 章）

#### 轮 1：品种级覆盖诊断基础（第一步）

- **目标**：`read_holdings` 逐品种标注数据状态（有行情 / 净值缺失 / 可能退市 / 代码格式可疑 / 名称不匹配），产出品种状态清单。
- **实施内容**：`core/reader.py` 增加品种状态标注逻辑（复用行情/净值接口返回状态 + 本地名称比对）；不改变持仓解析主流程，新增状态字段随解析结果输出。
- **量化验收标准**：
  - [ ] 新增单元测试 **≥8 个**（正常品种 / 净值缺失 / 退市码 / 格式错码 / 名称不匹配 / 空账户 / 多账户 / 边界），标注 `unit_*` marker，全部通过；
  - [ ] 新增模块覆盖率 **≥85%**；
  - [ ] **行为断言**：构造 5 品种持仓（含 1 个格式错码、1 个净值缺失），状态清单准确标注全部异常；
  - [ ] 向后兼容：正常持仓的状态标注不改变解析结果（旧断言全绿）；
  - [ ] §2 通用标准全部满足。

#### 轮 2：18 章改造第一二区块（源健康 + 品种覆盖）

- **目标**：18 章「数据源可用性矩阵」扩展为前两区块——源级健康（现状保留）+ 品种级覆盖（轮 1 状态清单）。
- **实施内容**：`report/excel_sheet_factory.py` / 18 章渲染模块加区块结构；品种覆盖列由轮 1 状态清单驱动；config 开关 `report_submodules.data_quality`（默认关）。
- **量化验收标准**：
  - [ ] 新增/修改测试 **≥6 个**（区块渲染 / 品种覆盖列 / 源健康保留 / 降级占位 / 开关关隐藏），全部通过；
  - [ ] **行为断言**：构造含异常品种的持仓生成报告，18 章正确列出品种异常行；开关关闭时 18 章回归旧样式（差异断言）；
  - [ ] 覆盖率对新增渲染模块 **≥85%**；
  - [ ] 向后兼容：开关默认关，既有 18 章输出不变；
  - [ ] §2 通用标准全部满足。

#### 轮 3：18 章改造第三区块（可信度 + 异常跳变）（第二步）

- **目标**：数据新鲜度摘要 + 净值/价格单日 ±20% 异常跳变检测，报告头部给"N 个品种数据异常"摘要行。
- **实施内容**：缓存/降级元数据（`data_status.py`、`datasource_health.jsonl`）聚合为可信度摘要；新增跳变检测（复用历史/净值数据，阈值 ±20%）。
- **量化验收标准**：
  - [ ] 新增测试 **≥8 个**（新鲜度计算 / 跳变检测阈值 / 非交易日不误报 / 降级状态 / 过期缓存 / 摘要行），全部通过；
  - [ ] **行为断言**：构造净值单日 +25% 突变，报告正确标注"疑似数据错误"；摘要行正确计数；
  - [ ] **计算精度**：**固定 fixture** 构造实时/缓存/降级/过期状态，分类结果与预期一致（自动化断言，非手工核对）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

### 阶段 B 决策中枢（轮 4~7，新增 17 章行动建议 + 12 章摘要）

> **宿主说明**：调仓建议/交易纪律/收益归因是纯算法功能，宿主为 `always` 类型的**独立顶层章节 17 章「行动建议」**（basic/both/full 均可见，`enable_action` 默认关）；12 章（LLM 场景）以「行动摘要」引用 17 章结果，**单源计算、两处呈现**，避免重复计算。详见设计 §4.2 行动建议独立成章。

#### 轮 4：新增 17 章「行动建议」框架 + 12 章行动摘要

- **目标**：新增 `always` 类型独立顶层章节「行动建议」（第 17 章）框架（再平衡信号 + 交易纪律 + 调仓建议 + 收益归因）；12 章加「行动摘要」子块（引用 17 章）。
- **实施内容**：`registry.py` 注册 `action` 章节（`_REPORT_SHEET_NAMES` 加 `"action": "行动建议"` + `_REPORT_SECTION_DEFAULT` 序号 17）+ 顶层开关 `enable_action`（默认关）；行动板块渲染框架；17 章与 12 章共享同一计算结果（模块级单次计算，两处引用）；12 章摘要为只读引用，不重复计算。
- **量化验收标准**：
  - [ ] 既有报告测试**全部回归通过**（新增章节不破坏现状）；
  - [ ] 新增结构测试 **≥5 个**（17 章渲染 / 12 章摘要 / 开关默认关 / 章节注册与序号 / 单源计算断言），全部通过；
  - [ ] **行为断言**：`enable_action` 关时报告无 17 章、12 章与现状一致；开时 17 章显示完整行动板块、12 章显示摘要（diff 断言）；
  - [ ] **单源计算断言**：17 章与 12 章行动数据为同一对象引用（无重复计算，测试断言同一实例）；
  - [ ] **向后兼容**：开关默认关时报告为合并后 18 章基线（无行动建议），既有章节输出不变；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 5：交易纪律（止盈/止损/回撤）

- **目标**：纪律触发检测——按成本价止盈/止损线、组合回撤纪律；与既有「再平衡信号」同构扩展（同"触发/未触发 + 距触发幅度"）。
- **实施内容**：新增纪律规则模块（止盈/止损/回撤三类规则，参数化），输出到 17 章行动建议纪律子块（12 章摘要自动带上）。
- **量化验收标准**：
  - [ ] 新增测试 **≥10 个**（止盈触发 / 止损触发 / 距触发幅度 / 回撤纪律 / 参数边界 / 静默期 / 多品种），全部通过；
  - [ ] **行为断言**：构造 -15% 止损场景，正确输出"触发 + 距触发幅度 + 建议动作"；
  - [ ] 复用既有再平衡结构，不重复实现（代码复用断言：无新增重复的触发判断逻辑）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 6：调仓建议清单（含可行化层）

- **目标**：17 章行动建议输出「调仓建议清单」——每条含代码/名称/操作/份额（取整到一手）/金额/预估费用/调仓后现金余额。
- **实施内容**：可行化层模块——份额取整（A 股一手 100 股、基金取整）、现金缓冲约束、税费估算（复用调仓成本模拟费率表）、优先级排序。
- **量化验收标准**：
  - [ ] 新增测试 **≥12 个**（份额取整一手 / 现金负值防护 / 费用计算 / 优先级 / 多品种 / 边界），全部通过；
  - [ ] **行为断言**：构造持仓，建议"X 份可一手成交、调仓后现金 ≥0、费用计算正确"，逐条断言；
  - [ ] 计算精度：费用估算与费率表**固定 fixture**（含小数手续费）误差 <0.01 元（自动化断言，非手工核对）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 7：收益归因（品种收益贡献占比）

- **目标**：组合收益按品种贡献排序——TOP 5 盈利/亏损来源（贡献占比 pp，非收益率），正负分列 + 正负合计摘要。
- **实施内容**：归因模块**复用 v0.7.x 已实现的 `_build_profit_attribution_block`**（`llm/prompts_core.py`，本地品种盈亏直接算，零新增数据源），输出到 17 章行动建议归因子块（12 章摘要自动带上）。**范围边界**：只做「品种收益贡献」归因，**不做 Brinson 分解**——Brinson 已在 v0.7.x §4.2 不做清单与 v0.9.x plan-4 归档**两次放弃**（3/4 关键输入不可突破：行业指数 K 线不稳 / 基准行业权重无免费源 / 非 A 股分类）。
- **量化验收标准**：
  - [ ] 新增测试 **≥6 个**（贡献占比 / 正负分列 / 合计摘要 / Σ|profit|=0 保护 / 不足 5 只 / 边界），全部通过；
  - [ ] **计算精度**：**固定 fixture** 构造已知盈亏，贡献占比与解析解误差 <0.01%（自动化断言，非手工核算）；
  - [ ] **行为断言**：构造 5 只品种（3 盈 2 亏），正确输出盈利来源/亏损来源/净额；Σ|profit|=0 时返回空段落（不报错）；
  - [ ] **复用断言**：直接复用 `_build_profit_attribution_block`，无重复实现；
  - [ ] 覆盖率对新增/改造模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

### 阶段 C 历史增强（轮 8~10，改造 15 组合历史走势与回撤 / 16 组合演进）

#### 轮 8：危机区间标注（15 章）

- **目标**：15 章「组合历史走势与回撤」净值曲线危机区间分段着色（2015/2018/2020/2022）+ 区间统计（区间回撤/恢复天数）。
- **实施内容**：危机日期静态表 + 标注层；复用既有历史数据管道，不额外拉长 lookback（以 `history.lookback_days` 为准）。
- **量化验收标准**：
  - [ ] 新增测试 **≥6 个**（区间标注 / 区间回撤 / 恢复天数 / 数据不足占位 / 无历史区间），全部通过；
  - [ ] **行为断言**：构造含 2018/2020 区间数据，正确标注并统计；无历史区间显式"无历史数据"；
  - [ ] 复用既有历史数据，不新增 lookback 拉长（无新增网络请求类型断言）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 9：尾部风险统计（15 章）

- **目标**：15 章加尾部指标列——VaR(95/99)、最大单日跌幅、连续下跌天数、下跌后恢复时间。
- **实施内容**：尾部风险计算模块（复用历史日收益序列），输出为 15 章指标列。
- **量化验收标准**：
  - [ ] 新增测试 **≥8 个**（VaR95/99 / VaR 参数 / 最大单日跌幅 / 连续下跌 / 恢复天数 / 样本不足），全部通过；
  - [ ] **计算精度**：**固定 fixture** 构造已知收益序列，VaR(95) 与解析解误差 <0.01%（自动化断言，非手工核对）；
  - [ ] **行为断言**：构造含大单日跌幅序列，正确输出 max 单日跌与恢复天数；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 10：快照差异摘要（16 章）

- **目标**：16 章「组合演进」顶部加「自上次快照变化摘要」（集中度/相关性/超限项/新增移除品种）。
- **实施内容**：快照差异模块——对比两次快照结构；复用既有多快照数据（`data/history/snapshots/`）。
- **量化验收标准**：
  - [ ] 新增测试 **≥6 个**（差异计算 / 新增移除品种 / 集中度变化 / 超限项 / 无上次快照占位），全部通过；
  - [ ] **行为断言**：构造两快照，摘要准确列出全部变化点（新增/移除/超限）；
  - [ ] 无上次快照时正确占位（不报错）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

### 阶段 D 风格与选基（轮 11~12，合并 9 章 + 改造 5 章）

#### 轮 11：合并 9 章「风格与因子分析」+ 行业 Beta 子表

- **目标**：合并原「基金风格分析」+「因子暴露分析」为「风格与因子分析」（分「基金风格表 + 风格因子回归」两区块），并加「行业 Beta」子表——穿透行业暴露占比 + 各行业指数 Beta/相关性。
- **实施内容**：合并 9 章两个渲染模块（基金风格表 + 因子回归区块）；行业 Beta 子表（穿透行业分类 push2 + 行业指数历史 K 线腾讯/新浪），复用 9 章 OLS 回归机制。
- **量化验收标准**：
  - [ ] 新增测试 **≥10 个**（合并后两区块渲染 / 行业暴露占比 / Beta 回归 / 显著性 / 数据不足 / 开关关隐藏 / **push2 行业分类降级**），全部通过；
  - [ ] **计算精度**：**固定 fixture** 构造已知收益序列，行业 Beta 与解析解误差 <0.01（自动化断言，非手工核对）；
  - [ ] **数据源降级**：push2 行业分类不可用时，行业暴露占比显示"数据不足"占位，Beta 子表不渲染，不阻塞 9 章其余内容；
  - [ ] **合并断言**：合并后旧「基金风格分析」「因子暴露分析」独立 sheet 不再生成（回归 diff），两区块同章渲染；复用 OLS 回归机制，无重复实现；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 12：候选基金比较增强模式（5 章）

- **目标**：5 章「基金业绩分析」加「候选比较」子模式——候选基金横向比较表（规模/费率/排名/收益/回撤/风格/与现有持仓重合度）。
- **实施内容**：候选来自 config `comparison_candidates` 或 CLI 参数（≤10）；开关 `report_submodules.candidate_compare` **默认关**。
- **量化验收标准**：
  - [ ] 新增测试 **≥8 个**（候选表渲染 / 比较维度 / 开关默认关 / 无效候选容错 / 超 10 限制），全部通过；
  - [ ] **行为断言**：开关默认关时 5 章无比较子表；开启并给候选后正确渲染比较表；
  - [ ] 候选 >10 时拒绝（上限断言）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

### 阶段 E 成本流水（轮 13~15，输入扩展 + 改造 1/2/3 章）

#### 轮 13：持仓文件格式扩展（交易流水/分红流水页签）

- **目标**：持仓 Excel 新增**可选**「交易流水」「分红流水」页签（不破坏既有 4 列）；无流水回退聚合成本，向后兼容。
- **实施内容**：`core/reader.py` 支持可选附加页签解析 + 格式校验；CLAUDE.md「列名不可配置」约束不触及（扩展的是附加页签，不改 4 列语义）。
- **量化验收标准**：
  - [ ] 新增测试 **≥10 个**（有流水 / 无流水回退 / 流水格式校验 / 多账户 / 非法流水容错），全部通过；
  - [ ] **向后兼容**：旧格式文件（无流水）解析结果与改造前**逐字段一致**（回归 diff 断言）；
  - [ ] 既有 4 列解析逻辑零改动（代码 diff 断言）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 14：资金加权收益（XIRR）+ 成本分档计算

- **目标**：资金加权收益率（XIRR）计算 + 分笔成本分档（低成本/高成本档），纳入分红现金流时点效应。
- **实施内容**：XIRR 计算模块（现金流时点迭代求解）+ 成本分档模块（结合流水）。
- **量化验收标准**：
  - [ ] 新增测试 **≥10 个**（XIRR 已知案例 / 定投场景 / 一次性买入 / 分红时点 / 分档 / 边界），全部通过；
  - [ ] **计算精度**：已知年化 10% 定投案例，XIRR 计算**误差 <0.1%**（收敛阈值断言）；
  - [ ] **行为断言**：含分红现金流时，XIRR 正确纳入时点效应（对比不含分红结果）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 15：2/3/1 章渲染成本分档 + 分红现金流累计

- **目标**：3 章加「成本分档」「分红累计」子列；2 章加可选「资金加权成本」列；1 章加「XIRR」汇总行。
- **实施内容**：渲染扩展——开关 `report_submodules.cost_lots`（**默认关**，有流水才建议开）。
- **量化验收标准**：
  - [ ] 新增/修改测试 **≥8 个**（子列渲染 / 开关 / 分红累计 / XIRR 行 / 无流水占位），全部通过；
  - [ ] **行为断言**：开关默认关时 1/2/3 章与改造前一致；开关开 + 有流水时正确显示分档/XIRR；
  - [ ] 覆盖率对改造模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

### 阶段 F 估值与温度（轮 16~17，改造 4/1 章）

#### 轮 16：估值分位（4 章）

- **目标**：4 章「资产穿透TOP10」加「估值分位」列——当前 PE/PB（push2 扩展字段）+ 3~5 年价格分位代理，显式标注"价格分位代理，非真实历史估值分位"。
- **实施内容**：估值分位模块——push2 当前值 + 历史 K 线价格分位计算（腾讯/新浪）。
- **量化验收标准**：
  - [ ] 新增测试 **≥8 个**（PE/PB 获取 / 价格分位 / 代理标注 / 数据不足 / 开关关隐藏 / **push2 不可用时降级**），全部通过；
  - [ ] **计算精度**：**固定 fixture** 构造已知 3 年 K 线，价格分位与解析解误差 <0.5%（自动化断言，非手工核对）；
  - [ ] **行为断言**：低估/合理/高估三档刻度正确映射，代理标注"价格分位代理，非真实历史估值分位"显式出现；
  - [ ] **数据源降级**：push2 扩展字段不可用时，估值分位列显示"数据不足"占位，不阻塞 4 章其余内容（复用既有降级链路）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

#### 轮 17：市场温度刻度（1 章）

- **目标**：1 章「投资分析汇总」加「市场温度」刻度行——价格分位 + 均线偏离 + 波动率三因子合成，输出"温度计"（低估/合理/高估）而非仓位指令。
- **实施内容**：市场温度模块（指数历史 K 线，腾讯/新浪）+ 免责声明；复用轮 16 价格分位机制（**依赖轮 16**，见 §4.1 轮次依赖）。
- **量化验收标准**：
  - [ ] 新增测试 **≥8 个**（温度计算 / 三因子合成 / 刻度映射 / 免责声明 / 数据不足占位 / **K 线源降级** / 开关默认关隐藏），全部通过；
  - [ ] **计算精度**：**固定 fixture** 构造已知指数 K 线，温度值与解析解误差 <0.5%（自动化断言，非手工核对）；
  - [ ] **行为断言**：输出温度计三档，无"建议几成仓"指令性文案（合规断言，负向断言）；
  - [ ] **数据源降级**：腾讯 K 线不可用时自动切新浪，两者都不可用则"数据不足"占位（复用既有 K 线降级链）；
  - [ ] 覆盖率对新增模块 **≥85%**；
  - [ ] §2 通用标准全部满足。

### 阶段 G 导航与收尾（轮 18~20）

#### 轮 18：HTML 分组标签与导航折叠

- **目标**：HTML 报告按「基础/基金深度/风险/历史/LLM」分组导航折叠；Excel 沿用 `report_section_order` 自定义排序。
- **实施内容**：HTML 模板分组导航 + 折叠交互；适配合并后的章节结构（「持仓关系矩阵」「组合历史走势与回撤」「风格与因子分析」为合并章，行动建议章在 `enable_action` 开启时纳入导航）。
- **量化验收标准**：
  - [ ] 新增测试 **≥4 个**（分组渲染 / 折叠交互 / 移动端不溢出 / 键盘可达），全部通过；
  - [ ] **行为断言**：章节分组正确（含合并后持仓关系矩阵、组合历史走势与回撤、风格与因子分析；`enable_action` 开时含行动建议章）、导航可达、折叠展开正常；
  - [ ] 章节内容不因分组/折叠改变渲染（回归断言）；
  - [ ] §2 通用标准全部满足。

#### 轮 19：文档、数据快照与用户手册

- **目标**：管理文档版本头一致、数据快照刷新、用户手册补充新子模块说明。
- **实施内容**：`folders.md` 目录树 / `test-coverage.md` 计数（`collect-test-coverage.py` 刷新）/ `reports-instruction.md` 新子模块说明 / `how-to-config.md` 新 config 开关 / `datasource*.md` 数据源一致性 / `changelog.md` 汇总。
- **量化验收标准**：
  - [ ] `python scripts/check-version-consistency.py` **全部 13 项 [OK]**（版本头一致）；
  - [ ] `collect-test-coverage.py` 刷新后 `test-coverage.md` 计数与实时一致（脚本 diff 断言）；
  - [ ] `folders.md` 目录树含全部新增/修改文件，统计表更新；
  - [ ] 用户手册覆盖新增子模块（reports-instruction / how-to-config 含新开关说明）；
  - [ ] `check-doc-traces --ci`、`check-task-numbering --ci` 通过；
  - [ ] §2 通用标准全部满足。

#### 轮 20：全链回归与发布门禁

- **目标**：全量验证 + 手动验证清单 + 发布准备。
- **实施内容**：`test_runner.py --mode verify,regression` 全量；手动验证清单（真机构造持仓全流程）；review-findings 清理；版本号一致复核。
- **量化验收标准**：
  - [ ] `test_runner.py --mode verify,regression` **全部通过**（含全部新 marker）；
  - [ ] 3 个 `check-*.py --ci` 全部通过；
  - [ ] **手动验证清单**（真机构造持仓 → 全报告生成 → 新子模块逐项核对 → 预览/下载）逐项勾选无遗留；
  - [ ] review-findings 无 CRITICAL/HIGH 遗留（MEDIUM 以下可记录）；
  - [ ] 数据快照与版本号一致（check-version-consistency 全 [OK]）；
  - [ ] §2 通用标准全部满足。

---

## 4. 验收与发布流程

**每轮验收流程**（RED→GREEN→门禁→回填）：
1. 写测试（RED）：先按该轮"量化验收标准"写测试，标注正确 marker；
2. 实现（GREEN）：最小实现通过测试；
3. 覆盖率核查：涉及模块 ≥80%，新增 ≥85%；
4. 门禁：dev-verify + 3 check 脚本；
5. 回填：changelog 记一轮 + review-findings 处理；
6. **验收签字**：该轮全部"量化验收标准"勾选完成才进入下一轮。

### 4.1 轮次依赖

轮次间依赖关系（前置未达验收不得开始后续轮，已在各轮标注）：

| 依赖 | 关系 | 说明 |
|:--|:--|:--|
| 轮 4 → 轮 5/6/7 | 宿主先行 | 17 章行动建议框架 + 单源计算对象在轮 4 落地；轮 5/6/7 仅向框架内添加子块，12 章摘要自动带上，无需改宿主 |
| 轮 13 → 轮 14 → 轮 15 | 输入→计算→渲染 | 轮 13 流水页签解析是 14/15 前置；轮 15 渲染依赖 14 的 XIRR/分档计算 |
| 轮 16 → 轮 17 | 机制复用 | 轮 17 复用轮 16 价格分位机制（指数 K 线），不得自行重写 |
| 轮 8/9/10 | 平行 | 均复用既有历史数据管道与快照，互不依赖，可并行实施 |
| 轮 11/12 | 平行 | 复用既有 9 章回归与行情链，互不依赖 |

**章节互不冲突说明**：行动建议是独立顶层章节（17 章，`enable_action`），与 3 章成本分档（`cost_lots`）、4 章估值（`valuation_percentile`）、15 章尾部风险（`tail_risk`）等子模块分布在**不同章节**，开关独立互不影响。

**若某轮验收不达标**：不进入下一轮；拆分为子迭代继续，直到该轮验收项全绿（迭代轮次可扩展，20 轮为基线）。

**发布门禁（P2）**：轮 20 达标后，按 CLAUDE.md 发布门禁执行——`verify,regression` + 3 check + 版本号一致 + 数据快照刷新 + 打 tag。

---

## 5. 与 plan.md 衔接

- 本实施计划对应 `plan-investment-features.md` §4 章节归并方案，是**实施层**文档，不在 plan.md 展开细节。
- **立项**：评审通过后，各阶段在 plan.md 分配 `plan-N` 编号（当前 `plan-next = 17`，从 plan-17 起分配），每阶段一个计划项，链接本文档对应轮次区间。
- plan.md 当前 P3 待办（plan-8 Web UI / plan-10 日志可视化）与本计划**独立**；本计划全部为 CLI/TUI/报告层功能，不依赖 plan-8 落地（轮 18 HTML 分组为报告自带，非 Web）。
- 实施时按本文档 20 轮推进，每轮回填 changelog、自审 review-findings；完成归档至 `docs-stm/archive/v0.x.x/`（与版本发布同步）。
