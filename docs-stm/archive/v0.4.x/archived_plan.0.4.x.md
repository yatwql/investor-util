# 实现计划归档 — v0.4.x

> 归档时间：2026-07-14
> 原始文件：`docs-stm/managements/plan.md`
> 涵盖版本：v0.4.0 ~ v0.4.5

---

## ✅ G. 报告板块可见性可配置（v0.4.5 已完成）

报告模块的可见性控制从单一硬编码逻辑升级为双层可配置模型：
- **board 层**（用户配置）：`enable_b_series`/`enable_news`/`enable_history` 三个配置字段，通过 TUI 菜单 [P] 交互式启停
- **data 层**（运行时数据可用性）：数据获取阶段标记实际可用的数据维度，与 board 层 AND 合并决定最终可见性
- **连续重新编号**：隐藏模块不留下空洞，`llm_usage` 强制末位

**迁移**：
- G 任务从"待实现方向"移至"已完成迭代"
- H 智能预警模块去留评估新增为待实现方向
- 低价值任务 O 已移除

## F2 组合历史走势完成（v0.4.1）

F1 快照对比 + F2 历史走势功能上线：
- F1：每次报告生成自动创建持仓快照，环比输出差异摘要
- F2：as-if 模拟组合历史走势，Chart.js 折线图 + 回撤面积图
- 新增报告章节 #17 组合历史走势、#18 回撤分析

**迁移**：
- 历史走势从"待实现方向"移除，加入"已完成迭代"

## 其他 v0.4.x 重要变更

### v0.4.5
- `should_create_sheet` 重构（Option A）：去掉硬编码 type_map，改为 data_flag 查询
- `set_sheet_title` 死代码移除
- TUI 菜单 B/L 描述更新

### v0.4.2
- 00 代码基金行情/历史全链路修复
- CDN 链路优化 + 原生 Canvas 即时渲染
- 组合历史走势并行获取（ThreadPoolExecutor）
