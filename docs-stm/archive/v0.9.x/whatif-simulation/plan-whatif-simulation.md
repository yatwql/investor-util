# 调仓 What-if 模拟（plan-5）

> **📦 已归档**：plan-5（调仓 What-if 模拟）已于 2026-08-03 完成（v0.9.8 发布）并归档至本目录。
> 原计划：`docs-stm/plan/plan-advanced-analysis.md` §2（原文件已裁剪，仅保留 plan-4 已放弃内容）。

## 目录

1. [调仓 What-if 模拟](#1-调仓-what-if-模拟)

---

## 1. 调仓 What-if 模拟

### 概述

用户调仓前想看到"改了之后报告什么样"。通过双目录镜像机制：修改持仓前拍快照，改后跑对比报告，展示 diff 视图。

### 收益

- **决策支持**：不是跑完报告才发现"哎呀，调仓后夏普反而变差了"
- **可视化 diff**：旧持仓 vs 新持仓的量化指标对比、行业分布对比、相关性变化
- **工作流闭环**：分析 -> 假设 -> 验证 -> 行动
- **技术基础**：已有 `history_snapshot` 快照机制，再扩展一步即可

### 风险

- 需要加载两份持仓数据同时计算，内存和缓存使用量翻倍
- What-if 的净值曲线没有历史数据（新持仓还没跑过市场），只能做截面比较
- 用户可能误用：What-if 的量化指标不能回测（没有真实交易数据）

### 工作量估算

| 阶段 | 内容 | 天数 |
|------|------|------|
| 镜像机制 | double-entry 目录 + 持仓快照 | 1 |
| 对比管线 | 编排层同时加载两份+对比计算 | 2 |
| 报告输出 | diff 视图（Excel 增删改标注 + HTML 对比表） | 2 |
| **合计** | | **5 天** |

### 实现思路

```
data/holdings/
├── current/              # 当前持仓（实时运行默认加载）
└── snapshot_20260720/    # 快照：调仓前的状态

对比报告输出：
- Excel：新增/删除/权重变化 用颜色标注
- HTML：双栏对比布局 + 指标变化箭头（↑ ↓ →）
```

### 数据源可行性分析

| 维度 | 结论 |
|:-----|:-----|
| 新数据源需求 | **无** — 纯结构层功能（双目录镜像 + 对比管线 + diff 输出），不新增任何网络请求 |
| 依赖的既有数据 | 现有管线全量数据（行情/穿透/指标/LLM），全部为 🟢 生产验证级别 |
| 数据源风险 | **无** — What-if 的局限是净值曲线无历史、不能回测，但这些是设计边界而非数据源问题 |

**结论**：plan-5 不依赖任何外部数据源，可行性无数据源阻力。
