# 投资复盘助手 — 需求文档
> 文档版本：0.11.1-dev

---

## 总体结构

本文档按**由全局到局部、功能到非功能**的顺序组织：

```
第一部分：功能需求
  ├─ §1 系统概述
  ├─ §2 运行环境
  ├─ §3 用户交互
  ├─ §4 持仓输入格式
  ├─ §5 数据获取
  ├─ §6 报告输出
  ├─ §7 LLM 智能分析
  └─ §8 错误处理与降级

第二部分：非功能需求
  ├─ §9 缓存策略
  ├─ §10 性能
  ├─ §11 配置
  └─ §12 约束与假设
```

---

# 第一部分：功能需求

---

## 1. 系统概述

### 1.1 目标

本系统读取用户持仓 Excel 文件，自动从多个公开金融数据源获取市场行情、基金业绩、财经新闻等数据，生成结构化的投资分析报告（Excel + HTML 双格式），并通过 LLM 提供智能分析增强。用户可通过 TUI 交互菜单、CLI 命令行或 Web 浏览器三种入口使用本系统（三种入口共用同一套管线、缓存与配置）。

### 1.2 核心业务流程

```
用户持仓 xlsx
     │ 读取
     ▼
多源数据并行获取（行情 / 基金 / 行业 / 新闻 / 历史走势）
     │ 汇总
     ▼
报告生成（Excel + HTML 双管线）
     │
     ├── 基础报告（E 菜单）：汇总、市值、分类、穿透、基金业绩、组合演进、数据源可用性矩阵
     ├── 完整报告（B 菜单）：基础 + 新闻 + 基金深度分析 + 历史走势
     └── LLM 增强报告（L 菜单）：完整报告 + 全球政经/智囊团/体检/穿透分析
```

> 以上三条报告路径由 TUI 菜单（E/B/L）、CLI 命令行（`--type basic/both/full`）与 Web 浏览器模式（报告格式下拉：基础/标准/完整）三种入口共用，共享同一缓存与配置。

---

## 2. 运行环境

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-ENV-01 | 基础源代码基于 Python 编写 |
| R-ENV-02 | Windows 11 使用 PowerShell 脚本启动，支持直接调用 Python 源代码或启动 TUI 界面 |
| R-ENV-03 | Linux 使用 Bash 脚本启动，支持直接调用 Python 源代码或启动 TUI 界面 |
| R-ENV-04 | Windows 11（`launch.ps1 web`）与 Linux（`launch.sh web`）均可启动 Web 浏览器模式：浏览器上传持仓、选择报告格式与选项、实时查看生成进度、在线预览/下载产物 |
| R-ENV-05 | 提供 CLI 便捷入口包装脚本 `scripts/cli.sh`（Linux/macOS）与 `scripts/cli.ps1`（Windows PowerShell）：自动定位项目虚拟环境解释器（`.venv/bin/python` / `.venv\Scripts\python.exe`）并切换到项目根目录；无参数调用时默认生成报告（`report --type both`，Excel+HTML 双格式、不含 LLM、全部页签有数据），传入参数时原样透传给 CLI |

---

## 3. 用户交互

### 3.1 启动与主界面

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-TUI-01 | 启动后显示标题"投资复盘助手" |
| R-TUI-02 | 主菜单提供以下 20 个交互选项 |
| R-TUI-06 | 主菜单启动后**缺省选中 `[L]`（生成完整报告）**——初始光标由 `config.json` 的 `default_menu_key` 决定（出厂值 `"L"`，取值须为 §3.2 中的任一菜单快捷键）；取值未命中时回落到首项 `[E]` |

### 3.2 菜单选项

| 选项 | 功能 | 说明 |
|:----:|:-----|:------|
| **报告生成** | | |
| E | 生成基础版 Excel 分析报告 | 投资分析汇总、持仓明细与分类、资产穿透TOP10、基金业绩分析、组合演进、数据源可用性矩阵（5 个始终显示 + 组合演进默认开启，由 `enable_portfolio_evolution` 控制；行动建议由 `enable_action` 控制，默认开启；不含基金深度分析/新闻/历史走势） |
| B | 生成标准报告（Excel+HTML）按章节配置 | 同时生成 HTML + Excel，含新闻（如有）、基金深度分析（如有）、历史走势（如有），不含 LLM 增补。各章节按 [P] 菜单配置决定是否包含 |
| L | 生成完整报告（Excel+HTML）含 LLM，按章节配置 | 同时生成 HTML + Excel，含新闻、基金深度分析、历史走势、LLM 增补。各章节按配置决定是否包含 |
| W | 调仓 What-if 模拟（对比两份持仓，独立报告） | 选择基准/目标两份持仓 xlsx，生成独立调仓 diff 报告（Excel 3+1 页签 / HTML），可选指定生效日触发时序回测（见 §6.11） |
| **配置管理** | | |
| C | 配置持仓信息目录 | 设置持仓 xlsx 文件的存放目录 |
| F | 配置持仓信息文件名 | 设置持仓 xlsx 的文件名 |
| O | 配置报告输出目录 | 设置报告输出目录（默认 `reports/`） |
| I | 管理对比指数池（自定义基准指数） | 交互添加/删除对比基准指数 |
| A | 配置持仓匿名化（代码/名称脱敏） | 交互式切换匿名化模式（关闭/代码显示/完全匿名/汇总） |
| P | 配置报告可选章节 | 交互切换基金深度分析/市场新闻/组合历史走势+回撤/组合演进/行动建议 5 个章节的启用/停用（报告章节与增强的 8 项开关归菜单 `[S]`「报告章节与增强」块，不在本面板） |
| S | 配置 LLM 分析章节 | 交互切换各 LLM 报告的启用/停用，含实验性辩论模式（⚗ 标识） |
| R | 刷新配置 | 重新加载 config.json / llm_settings.json / llm_key.json / llm_providers.json |
| **缓存管理** | | |
| 1 | 更新基础类缓存 | 主动更新基金业绩/持仓/基准/行业分类/新闻/盈利预测/资金流向/分红/基金经理/持仓重合度/风格扩展。纯股票持仓无基金时自动跳过基金项 |
| 2 | 更新持仓类缓存 | 主动更新价格/指数行情，清除关联 LLM 缓存 |
| 3 | 清理过期缓存文件 | 扫描 data/cache/ 目录，删除已过期的缓存文件 |
| 4 | 查看缓存/状态统计 | 分别显示 data/cache/（缓存文件总数/大小/命中率/前缀分布/过期预览）、data/history/snapshots/（历史快照数量/大小）、data/state/（运行时状态文件数量/大小）三项统计 |
| **诊断** | | |
| V | 查看最近运行日志 | 按级别筛选查看结构化运行日志（`core/log_reader.py`），无需生成报告；配置损坏时仍可查看（日志读取不依赖 config） |
| H | 查看数据源健康历史 | 查看近期数据源健康检查记录（`core/perf.py::summarize_health_history`），含 ok/total 与失败源清单 |
| D | 系统自检 | 环境/配置/目录/数据源一键体检（`core/doctor.py`，失败项附修复建议）；由开关 `doctor_check` 门控（默认开启，属常规组），置 false 时该菜单项就地裁剪 |
| **系统** | | |
| X | 退出 | 退出程序 |

### 3.3 操作流程需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-TUI-03 | 持仓目录下有多个 xlsx 文件时弹出选择器要求用户选择 |
| R-TUI-04 | 持仓目录不存在或无 xlsx 文件时弹出选择器要求配置持仓目录 |
| R-TUI-05 | 报告生成过程中显示进度提示（如"正在获取行情数据..."） |

### 3.4 Web 浏览器模式

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-WEB-01 | 通过 `launch.sh web`（Linux）/ `launch.ps1 web`（Windows）启动 Web 模式，默认监听本机回环地址 8000 端口（`--host`/`--port` 可改，绑定非回环地址需自行评估局域网暴露风险） |
| R-WEB-02 | 页面支持上传持仓 xlsx 文件（类型/大小校验，非法文件返回明确错误提示） |
| R-WEB-03 | 支持选择报告格式：基础（仅 Excel）、标准（HTML + Excel）、完整（HTML + Excel + LLM 增补），及「获取历史走势」「强制 LLM」选项 |
| R-WEB-04 | 报告生成过程中以事件流实时展示进度与当前状态 |
| R-WEB-05 | 生成完成后支持在线预览 HTML 产物与下载 Excel/HTML 产物 |
| R-WEB-06 | 同一时间仅执行一个报告生成任务，新任务进入排队（单 worker 串行队列） |
| R-WEB-07 | 上传文件与任务运行数据有生命周期管理：上传文件过期自动清理、运行记录按保留上限收敛 |
| R-WEB-08 | 页面提供配置编辑面板，覆盖 TUI 可编辑配置全集（路径 / 报告章节 / 报告章节与增强 `report_switches` / 匿名化 / 对比指数池 / LLM 开关 / 功能开关——界面上分「实验性功能」「常规开关」「报告章节与增强」三块）：修改即写共享配置文件（config.json / llm_settings.json / features.json）并写前自动备份 `.bak`，TUI/CLI 下次读取即生效 |
| R-WEB-09 | 报告生成提供「试算 / 正式」两种输入模式（试算默认）：试算读上传临时文件、快照写入 `web/` 隔离域，不落正式持仓、不污染共享时间线；正式 + 上传先提升临时文件为正式持仓（旧文件备份 `.bak`）再生成，正式 + 用存量直接读正式持仓文件（路径仅从配置派生，无目录穿越向量）；正式模式快照写共享主目录，TUI/CLI 可对比 |

### 3.5 日志可视化（诊断）

日志查看与数据源健康历史为诊断类功能，三端共用核心层 `core/log_reader.py`（结构化日志读取）+ `core/perf.py::summarize_health_history`（健康历史聚合），渠道层仅做薄展示、不含解析逻辑。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-DIAG-01 | CLI 提供 `view-logs` 子命令查看结构化运行日志：`--level`（最小级别阈值）、`--lines`（读取末尾行数上限，默认 5000）、`--since/--until`（时间前缀过滤）；无需 config（配置损坏时仍可查日志诊断）；输出每条 `time [LEVEL] message` |
| R-DIAG-02 | TUI 主菜单提供「V 查看最近运行日志（可按级别筛选）」「H 查看数据源健康历史（近期检查记录）」两项：级别筛选、ERROR/WARNING 着色（NO_COLOR/TTY 检测自动降级无着色）、traceback 折叠为「⤷ 堆栈详情 +N 行」 |
| R-DIAG-03 | Web 提供 `GET /api/logs` 与 `GET /api/health/history` 接口及「⑦ 日志查看」卡：级别校验（非法 400）、`lines` clamp [1,5000]、`since/until` 透传、读取失败 500；前端手动加载不自动轮询，`<details>` 原生折叠 + 级别配色，全程 `textContent`（防 XSS） |

### 3.6 系统自检（诊断，开关 `doctor_check` 默认开启）

一键盘点运行环境，三端共用核心层 `core/doctor.py`，渠道层仅做薄展示。分组输出（环境/配置/目录/功能开关/数据源适配/数据源凭据/数据源七组，顺序由 `GROUP_ORDER` 锁定），**失败项附可执行修复建议**。**自检自身永不抛异常**（异常一律转为该组失败行）、**零重依赖**（不 import pandas——其缺失正是它要报告的场景）。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-DIAG-04 | CLI 提供 `doctor` 子命令：`--offline` 跳过联网检查、`--timeout SECONDS` 限定单次检查预算；**无需 config 且在 `init_config` 之前分派**——配置损坏正是它要诊断的场景，被开关或配置初始化拦住会形成死锁；**不受 `doctor_check` 开关约束**。退出码 `0`=全部通过、`1`=命令跑完但有失败项（≠ 命令自身失败） |
| R-DIAG-05 | TUI 主菜单提供 `[D]` 系统自检项（默认出现），**由开关 `doctor_check` 门控**：`features.json` 中置 `false` 时该项由菜单门控就地裁剪，不出现在主菜单中 |
| R-DIAG-06 | Web 提供 `GET /api/doctor` 接口（`network=0` 跳过联网、`timeout` 上限 15s、非法值回落默认）及运行状态区「系统自检」卡片（默认显示），卡片可见性由 `system_info["doctor_enabled"]` 控制（同上开关）；接口在配置损坏时仍返回 200 并给出失败结果行 |
| R-DIAG-07 | 自检为只读诊断：不改动任何文件、不写产物，联网检查每次由调用方显式确认（TUI 询问后按需联网、Web 由按钮触发）。故其开关默认开启——默认关闭的代价是最需要它的人（环境故障者）恰好看不到它。开关属**常规组**（在 TUI 菜单 `[S]` 与 Web 配置面板的「常规开关」块中，非实验块），亦可改动 `features.json` |

---

## 4. 持仓输入格式

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-HLD-01 | 输入文件为 xlsx 格式，每个页签 = 一个账户，页签名即账户名 |
| R-HLD-02 | 每个页签固定 4 列：名称（str）、代码（str）、持仓份额（float）、每份成本（float） |
| R-HLD-03 | 列名固定，无需用户配置映射关系 |
| R-HLD-04 | 列顺序固定：名称 → 代码 → 持仓份额 → 每份成本 |
| R-HLD-05 | 代码自动去除 sh/sz/bj 前缀，统一为纯净 6 位数字代码 |

---

## 5. 数据获取需求

### 5.1 数据源总览

系统需从以下公开数据源获取数据。每个用途需指定主链路与备用链路，主链路失败时自动切换备用。

| 用途 | 主链路 | 备用链路 |
|:-----|:-------|:---------|
| 场内 A 股/ETF 实时价 | 腾讯财经 | 新浪财经 |
| 场外基金净值 | 东方财富 | — |
| 基金业绩排名 | 天天基金 | — |
| 基金持仓数据 | 天天基金 → 同花顺金融数据（官方披露持仓，备源；需 key） | — |
| A 股指数行情 | 腾讯财经 | 新浪财经 |
| 美股指数行情 | 新浪财经 | 腾讯财经 |
| 财经新闻（5 源并行） | 新浪/东方财富/财联社/华尔街见闻/akshare | — |
| 行业分类/概念板块 | 东方财富 push2 | 东方财富 REST 行情页回退 |
| 机构盈利预测 | akshare | — |
| 行业资金流向 | akshare | — |
| 股票历史分红 | akshare | — |
| 基金经理数据 | 天天基金 | 档案页回退 |
| 无风险利率（Rf） | akshare `bond_zh_us_rate`（Sina 国债收益率） | 手动配置兜底（config.json risk_free_rate） |
| 个股/ETF 历史 K 线 | 腾讯财经 | 新浪财经 |
| 场外基金历史净值 | 天天基金 | 东方财富 |
| 指数历史 K 线（A 股指数） | 腾讯财经 | 新浪财经 |
| 指数历史 K 线（美股指数） | 新浪财经 | 腾讯财经 |

### 5.2 数据质量需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-DATA-01 | 所有外部数据源必须配置备用链路或降级方案，单源不可用时不得中断报告生成 |
| R-DATA-02 | 数据获取失败时必须静默降级而非抛出异常，失败的资产在汇总日志中列出 |
| R-DATA-03 | 同一会话内同一 API 数据应复用，避免重复请求导致 API 限频 |
| R-DATA-04 | 盘中 A 股行情需使用短 TTL（默认 30s）确保实时性，盘后回落常规 TTL |
| R-DATA-05 | 收盘后需验证缓存中 price_date 是否为最近交易日数据；验证不通过（跨日残留）时强制清除缓存并重新获取最新净值写回，避免盘中降级残留数据滞留 |
| R-DATA-06 | 00 开头的代码同时可能是 A 股或场外基金，需通过名称关键词辅助判断，股票链路全失败时自动尝试基金净值链路 |

### 5.3 熔断与降级需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-BRK-01 | 单股票级 API 连续 3 次传输级失败后应熔断，冷却期满后自动试探恢复 |
| R-BRK-02 | 批量 API（如行业分类）连续 6 次传输级失败后应熔断 120 秒 |
| R-BRK-03 | API 正常返回空数据（非传输级异常）不计入熔断计数器 |
| R-BRK-04 | LLM API 端点应有独立的熔断器（连续 3 次失败熔断 60 秒） |
| R-BRK-05 | 同一次会话中跨模块的同一 API 调用结果应共享会话级内存缓存（上限 2000 条/domain） |
| R-BRK-06 | 熔断冷却时间应采用指数退避策略（60s→300s→900s→3600s），每次熔断冷却按退避级别递增一档（非严格翻倍、非按失败计数），成功恢复后重置 |
| R-BRK-07 | 熔断器状态应跨会话持久化到 `data/state/circuit_breaker.json`，与缓存文件隔离，确保会话重启后熔断记忆恢复 |
| R-BRK-08 | Provider 级熔断器和数据模块级熔断器应通过统一的网关（`core/circuit_breaker.py` + `core/provider_registry.py`）管理，消除双熔断器状态不一致

### 5.4 新闻获取需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-NWS-01 | 每个新闻源独立获取（合并前），各源原始获取量动态计算为 max(500, news_top_count × 2) |
| R-NWS-02 | 最终输出按关联度排序，截取 news_top_count 条（默认 300 条） |
| R-NWS-03 | 新闻关键词来源：持仓名称片段 + 代码 + 穿透资产名称 + 行业分类 + 概念板块 |
| R-NWS-04 | 关键匹配结果需在报告中以颜色区分来源类型（持仓名称=蓝色、穿透资产=紫色、概念=橙色、行业=灰色） |
| R-NWS-05 | 各新闻源可通过配置独立启停 |
| R-NWS-06 | 新闻模块可选的 LLM 二次关联分析 -> 提供关联度评级（高/中/低/无关）+ 情感分析（利好/利空/中性） |

### 5.5 历史走势数据获取需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-HST-01 | 场内股票/ETF 历史 K 线使用腾讯财经，备用新浪财经 |
| R-HST-02 | 场外基金历史净值使用天天基金，备用东方财富 |
| R-HST-03 | 数据获取支持增量合并：首次全量获取，后续仅拉取缺失日期（以已有缓存为底座） |
| R-HST-04 | 检测到历史数据修正（新数据与缓存日期重叠）时应自动全量刷新 |
| R-HST-05 | 数据获取模式由用户配置控制：off（关闭）/ prompt（询问后执行）/ auto（自动执行） |
| R-HST-06 | 00 代码的 K 线全空时自动降级为基金净值链路 |
| R-HST-07 | 基准指数历史数据通过 history_index（A 股）/ history_index_us（美股）chain 获取，支持增量缓存合并。美股指数（gb_ 前缀）路由至 history_index_us（新浪→腾讯）；两条链共用同一指数 K 线函数，无实现的 provider 被跳过——新浪侧实现虽在，但其 getKLineData 端点对全部代码返回 404/空，实际取数通常由腾讯承担，而腾讯 K-line API 对 gb_* 支持有限，故该链可能整体取空（空结果按正常降级记录） |

### 5.6 指数数据获取需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-IDX-01 | A 股指数主链路腾讯财经，备用新浪财经 |
| R-IDX-02 | 美股指数主链路新浪财经（JS 变量解析），备用腾讯财经 |
| R-IDX-03 | 双链路均失败时降级过期缓存 |

### 5.7 数据源适配契约（`datasource_adapter`，默认开启）

接入一个数据源只做三件事，且**声明在前、代码在后**：参数转译 → 抓取 → 映射到标准字段。同一数据源域的**标准字段只有一份**（字段名 + 类型即缺省语义），字段改名/缺省以适配器声明表达，替代各 provider 手拼 dict。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-ADP-01 | **三段式契约**：`transform_query`（参数转译，默认恒等）/ `extract_data`（抓取，既有源委托既有 provider 函数，不复制 HTTP 与解析逻辑）/ `transform_data`（映射到标准字段） |
| R-ADP-02 | **声明式归一**：默认映射由 `aliases`（上游字段→标准字段）、`defaults`（该源缺省值）与标准字段记录类的**类型注解**三样数据驱动——`float` 缺失取 `0.0`、`float \| None` 取 `None`（表示该源不提供此字段）、`str` 取空串；数值一律经数值归一拦下 NaN/±inf；「上游未提供」与「提供了但不可用」同待遇；合法的 `0.0` 不被缺省覆盖 |
| R-ADP-03 | **输出恒为全部标准字段**：同一域的不同源产出同键同构 dict，下游无需为「某源少两个键」写分支；源身份字段由适配器强制提供（上游自报的来源不覆盖链路标签，防「来源」与「实际生效链路」脱钩） |
| R-ADP-04 | **接入链路不新造路径**：适配器映射为 Provider Chain 的两槽（`provider_fn_map` / `transform`），链路顺序、缓存键、熔断、降级全部复用既有 `fetch_with_fallback` |
| R-ADP-05 | **试点范围与等价性**：仅行情域三源（腾讯/新浪/东方财富），与既有转换函数逐源等价，唯一有意差异为东方财富既有转换函数不含 `market_cap`/`pe` 而契约恒为全字段（补 `None`，下游一律 `.get()` 读取，取值语义不变）；**存量 provider 不回改**，新数据源/新字段先走契约 |
| R-ADP-06 | **开关关闭时行为逐字节不变**：链路返回既有手写映射对象本身；默认开启时改用适配器映射 |
| R-ADP-07 | **离线契约自检**：`survey_adapters()` 核验域登记、`aliases`/`defaults` 指向真实标准字段、映射输出恰好标准字段集，自身不抛异常、不发起网络请求；`doctor` 的「数据源适配」组报告适配器数量/各域/自检结论并标注契约路径是否启用——**开关关闭时也照常核验**（接入新数据源前正是要看这个） |
| R-ADP-08 | 开关 `datasource_adapter` **默认开启**——它是内部接缝，两种取值下报告数值不变（见 R-ADP-05），故不以默认关的形态占一个用户开关；开关保留为回退杠杆，`features.json` 置 false 即回退既有转换函数 |

### 5.8 数据源凭据就绪指引（常规开关 `datasource_credential_ready`）

把「此源需要什么凭据」**声明在数据里**，缺失时给出「缺什么 → 去哪申请」的可读指引，而不是等运行期拿到 HTTP 401 再当作「源不可达」反复重试。**多数数据源免费无需凭据；首个需凭据的源为 DataSinking 全文本财报**（`providers/datasink.py` 导入即登记 `CredentialSpec`）。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-CRD-01 | **声明即数据**：`CredentialSpec`（`source_id` / `display_name` / `env_var` / `apply_url` / `note` / `key_file` / `key_field` / `key_section`）登记进 `CREDENTIAL_SPECS`；接入需 key 的源时补一行 `register_credential_spec(...)` 即可，不新造第二套配置体系 |
| R-CRD-02 | **就绪判定**：凭据从声明的**通用密钥文件**（以 provider 名为节，如 `data/config/data_key.json` 的 `datasink` 节）或**环境变量**解析（环境变量优先）；**空白串（含纯空白/换行）视为缺失**（防「设了空值以为配好了」）；源未声明 → 不需要凭据 |
| R-CRD-03 | **可读指引**：缺失时给出「缺少凭据（数据源：X）——请填写密钥文件 PATH 的 FIELD 字段；或设置环境变量 VAR；申请地址：URL」，措辞与既有 LLM 凭据缺失指引同风格 |
| R-CRD-04 | **链路主动跳过且不进熔断**：`fetch_with_fallback` 与历史走势的 provider 遍历循环在熔断检查后做凭据预检，缺失则跳过该 provider 落到下一链路，**不计入熔断失败计数**（配置级问题≠源不可达），仅以可读原因进入 `FailureDiagnostics` 随降级事件上屏 |
| R-CRD-05 | **健康检查跳过态**：`check-sources` 对缺凭据的检查项**不发起探测**，直接产出 `⏭️` 跳过态并给出可读原因；跳过项**不计入 err/warn、不影响退出码**；开关开启时输出末尾追加就绪摘要行 |
| R-CRD-06 | **体检分组**：`doctor` 增设「数据源凭据」组——无声明报「当前无声明需凭据的数据源」，存在缺失则报失败项并附修复建议；「数据源」组过滤掉凭据跳过项，避免同一问题被两个组重复计为失败 |
| R-CRD-07 | **凭据值安全**：只从密钥文件/环境变量读取，**值永不落日志、永不写入报告与缓存**——日志与就绪矩阵只出现「来源类型 + 是否就绪」及申请地址 |
| R-CRD-08 | 常规开关 `datasource_credential_ready` 默认开启，**关闭时不产生任何行为分支**（链路照常尝试、无跳过项、无就绪行、无凭据组，输出与未引入本机制时逐字节一致）；三面上屏同源注册表（TUI 菜单 `[S]` / Web 配置面板 / CLI `--feature NAME=VALUE`） |

### 5.9 结构化财务指标数据域（`financial_indicator`）

把上市公司定期报告中的关键财务指标收敛为每报告期一条标准记录，作为「真实估值分位 + 质量因子」的数据底座；主源为 akshare（无需凭据），备用支路为从已缓存财报全文解析；第三链路为**同花顺官方合并报表派生**（三张报表各一次请求给多期，需 key）。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-FIN-01 | **独立数据域与标准字段契约**：新增 `financial_indicator` 域与 `FinancialIndicatorFields` 记录类（`code`/`symbol`/`report_period`/`doc_type`/`revenue`/`net_profit`/`revenue_yoy`/`net_profit_yoy`/`gross_margin`/`roe`/`debt_ratio`/`operating_cash_flow`/`eps`/`bvps`/`source_api`/`source`），登记 `DOMAIN_RECORDS` 并接受适配器契约自检 |
| R-FIN-02 | **主源（akshare）**：一次调用 `stock_financial_abstract` 取回宽表，按报告期归一为多条记录；金额类字段单位元、比率类字段百分数→**小数比例**；同名指标多分组时按表格顺序**首个非空**为准；非 A 股代码/未安装/超时/空表返回空 |
| R-FIN-03 | **接入既有链路**：新增链 `financial_indicator: [akshare_financial, datasink_indicator]` 与两个适配器；取数复用 `fetch_with_fallback`（缓存/熔断/降级），不新造获取路径；缓存前缀 `fin_indicator_` 纳入注册表（一月 TTL、基础类） |
| R-FIN-04 | **共享原语收敛**：A 股→FMP 符号映射统一为 `core/code_utils.py::to_fmp_symbol`，取数超时封装统一为 `providers/_utils.py::run_with_timeout`（多个 provider 共用，不自留副本） |
| R-FIN-13 | **数据底座门禁**：持仓基本面章区块①（财务指标）需 `datasink.enabled` 开启且凭据就绪（见 R-VAL-04）；未就绪时返回 None（章节隐藏、不写占位），且**门禁先于任何取数**（零网络开销） |
| R-FIN-05 | **降级不阻断**：指标源不可用时返回空/None，由上层按「数据缺失」占位；不抛异常、不计入传输级熔断 |
| R-FIN-15 | **PE/PB 官方口径优先**：指标标准记录新增 `pe_ttm`（TTM 市盈率）与 `pb_mrq`（MRQ 市净率）两字段；有官方值时 `current_valuation` 直接采用，缺失时逐字段回退「现价 ÷ EPS / BVPS」自算。理由：自算用**报告期**EPS，在半年报/季报上明显虚高（实测长江电力 自算 47.19 vs 官方 TTM 19.17，差约 2.5 倍），同一列混用会误导估值判断。官方估值只有当下快照 → **只写最新一期**，历史期不得倒填。同花顺估值快照另提供 `ps_ttm`/`pcf_ttm`（暂未纳入契约） |
| R-FIN-14 | **官方报表派生链路（第三槽）**：`analysis/financial_statement_derive.py` 把同花顺三张合并报表（利润表/资产负债表/现金流量表）纯派生为标准字段记录——毛利率/负债率/ROE 由报表原值派生（不臆造），同比与**上年同期**比较；**报告期按 `fiscal_year`+`fiscal_period` 归一到标准季末**（上游 `period_end_ms` 是披露窗口起点，直接用会与主源同报告期错位）；`bvps` 恒缺失（官方不给总股本，该源不产 PB）。主源不可用时先补足**多期**（保住趋势/质量档），再由链路单期兜底 |
| R-FIN-06 | **全文解析支路**：`datasink_indicator` 适配器逐章节试取 `公司简介和主要财务指标` → `主要财务数据`（**命中即止**，不重复取同章节），由 `analysis/financial_indicator_extract.py` 纯解析为同一标准字段集；正文为空/无可识别指标行 → None，继续降级 |
| R-FIN-07 | **解析护栏（宁缺勿错）**：锚点只用标准披露行文并对「扣除非经常性损益后的…」加否定环视；数值仅在锚点后有限窗口内取（超窗弃用）；按报告期精度选模式（金额两位小数、每股四位、比率两位）以免无分隔连写误切；金额取锚点前最近「单位：X」换算到元、**无声明即不产金额**；逐字段幅度/区间校验不通过取 None；同比由本期/上年同口径两值算术派生（不推断口径），无上年或上年为 0 → None；报告期一律由元数据给出 |
| R-FIN-09 | **报告消费区块**：「持仓基本面」章节的**区块① 财务指标**（Excel 同一页签 + HTML 同一 partial 内的区块，导航分组「基础信息」），功能开关 `financial_indicator` 默认关；数据驱动（开关关闭 → 该块整体不渲染；开关开启但无数据 → 写占位），不改变既有章节输出 |
| R-FIN-10 | **每行呈现**：最新报告期指标（营收/归母净利/同比/毛利率/ROE/负债率/经营现金流/EPS/每股净资产）+ **质量档**（四维均值启发式，非投资建议/非评级）+ **年度趋势**（相邻年报营收与净利）+ **当前 PE/PB**（现价 ÷ 每股收益/每股净资产；亏损或净资产非正留空）+ 近几期趋势点；金额列以亿元呈现、比率列以百分数呈现，缺失写「—」而非 0 |
| R-FIN-11 | **多期序列取数**：主源一次调用取多期；主源不可用时**退化为链路单期**（不伪造历史期），缓存前缀 `fin_indicator_hist_` 归入 `fin_indicator` 模块（一月 TTL）；现价取自行情明细，缺价即不产 PE/PB |
### 5.10 真实历史估值分位（TTM 口径，`valuation_percentile` 子模块）

把「价格自身分位」升级为「真实估值分位」：用历史 PE/PB 序列衡量贵不贵，需多期基本面（`financial_indicator` 域）与历史收盘价两个输入；无基本面覆盖时回落既有价格分位代理并如实标注口径。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-VAL-01 | **TTM 每股收益构造**：年报直接取当年 EPS；一季报/半年报/三季报按「上年年报 + 本期累计 − 去年同期累计」差分；所需期数不全 → 该期不产 TTM（不猜） |
| R-VAL-02 | **生效日 = 法定披露截止日**：年报/一季报 4-30、半年报 8-31、三季报 10-31（年报为次年）——**避免前视偏差**，某期数据仅在该日之后的历史价格上生效；同日生效（如年报与一季报）按报告期新者胜 |
| R-VAL-04 | **数据底座门禁**：持仓基本面章区块①与真实历史估值分位**以 DataSinking 数据底座就绪为前提**（配置位 `datasink.enabled` 开启 **且** 凭据已配置）；判定为**纯本地检查**（零网络请求、零延迟）。未就绪时两者**静默回原样**——该区块整体隐藏（不写占位）、估值列文案与免责语逐字保持引入前形态，不干扰阅读与业务分析 |
| R-VAL-03 | **分位与口径**：历史 PE 序列 = 逐交易日收盘价 ÷ 该日已生效最新一期 TTM EPS（TTM ≤ 0 即亏损不产 PE）；PB 序列 = 收盘价 ÷ 该期每股净资产（非正不产）；分位 = 历史值 ≤ 当前值占比，样本 < 60 判数据不足；**PE 优先、PB 兜底**并标注 basis；真实分位与价格代理在展示层分别标注（`DISCLAIMER_REAL` / `DISCLAIMER_PROXY`），不得混口径展示 |

| R-FIN-12 | **管线契约台账同步**：`financial_indicator_data` 登记于 `pipeline_data` 键表 + 类型断言表 + 技术文档附录 H，并纳入双端章节一致性测试（Excel 页签顺序/HTML 连续编号与注册表逐项对齐） |
| R-FIN-08 | **真实形态回归**：以真实年报章节夹具（`src/test/data/fixtures/`）锁定上游正文形态与逐字段核对值（含同比），配套合成样本与边缘场景（`*_edge.py`）测试；上游形态变化时重新取样并同步修订护栏 |

---

## 6. 报告输出需求

### 6.1 文件命名与存储

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-OUT-01 | 最新报告存放到 output_dir 根目录（默认 reports/） |
| R-OUT-02 | 存档报告存放到 {output_dir}/YYYYMMDD/ 子目录 |
| R-OUT-03 | Excel 文件名：最新"个人投资分析报告.xlsx"，存档追加日期时间戳 |
| R-OUT-04 | HTML 文件名：最新"个人投资分析报告.html"，存档追加日期时间戳 |

### 6.2 报告格式

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-OUT-05 | Excel 格式：每个模块独立一个页签，页签编号 1~19，数字前缀保证排序 |
| R-OUT-06 | HTML 格式：所有模块在同一页面中顺序排列，附目录锚点导航 |
| R-OUT-07 | Excel 页签编号和 HTML 章节序号支持通过配置 `report_section_order` 自定义：键=模块标识、值=序号，已配置模块按序号升序在前，未配置模块按默认顺序在后；空对象使用默认顺序（19 项） |
| R-OUT-08 | LLM API 用量页签强制固定在最后一位，不受排序配置影响 |
| R-OUT-09 | 各模块的条件渲染（是否显示）由两层控制：board 层配置开关 + data 层数据可用性 |
| R-OUT-10 | HTML 报告左侧提供可折叠目录（TOC）：桌面端固定左侧栏列出全部可见章节，点击平滑滚动定位到对应章节；TOC 可一键收起/展开（偏好持久化到 localStorage），滚动时高亮当前章节；窄屏（< 900px）自动隐藏、保留顶部横向 section-nav 导航；打印时隐藏 |
| R-OUT-11 | HTML 报告（主报告与调仓 What-if 报告）支持深/浅色主题切换：右上角浮动按钮切换，主题偏好持久化到 localStorage（首次默认浅色）；Chart.js 图表随主题重绘；打印时自动切浅色并隐藏切换按钮 |

### 6.3 报告模块清单

| 序号 | 模块名称 | 触发菜单 | 类型 | 说明 |
|:----:|:---------|:--------:|:-----|:-----|
| 1 | 投资分析汇总 | E/B/L | 基础报表 | 指数行情、账户汇总、持仓概况 |
| 2 | 持仓明细与分类 | E/B/L | 基础报表 | 同页签两区块：①市值明细 15 列（分账户小计+总计）②分类汇总 10 列（资产属性+投资分类聚合） |
| 3 | 资产穿透TOP10 | E/B/L | 基础报表 | 基金底层标的拆解合并排序 |
| 4 | 基金业绩分析 | E/B/L | 基础报表 | 同类排名、收益率、基准对比；含**基金经理变更监控块**（基金深度分析开启时渲染：快照式变更检测 1/3/6 月窗口三级预警） |
| 5 | 持仓结构与集中度 | B/L | 基金深度分析 | 一章三区块：重合度（Jaccard+重叠率双指标热力图）+ 相关性（下三角 Pearson 热力图+显著性标注）+ 集中度（top3/5/10 占比+环比变化+三级预警） |
| 6 | 风格与因子分析 | B/L | 基金深度分析 | 一章三区块：市值/PE 加权六宫格+漂移评分 + 风格因子回归（β 系数+基准对照+显著性）+ 行业 Beta 子表（功能开关 `industry_beta` 开关，默认关） |
| 7 | 行动建议 | E/B/L | 行动建议（enable_action 控制，**默认开**） | 再平衡信号/交易纪律/调仓建议/收益归因 |
| 8 | 财经新闻热点与持仓关联分析 | B/L | 新闻 | 5 源新闻关键词匹配，可选 LLM 增强 |
| 9 | 全球政经局势 | L | LLM | 基于指数+持仓结构生成 |
| 10 | 智囊团深度复盘 | L | LLM | 三阶段圆桌会议；Feature Flag 开启辩论模式（正反辩论/条件推理/集中度问答）时输出含辩论内容并标注"🧪 辩论模式"/"🧪 实验模式"标签 |
| 11 | 持仓体检报告 | L | LLM | 五维度量化评分 |
| 12 | 穿透深度分析 | L | LLM | 行业集中度+国别暴露 |
| 13 | 组合历史走势与回撤 | B/L | 历史 | 一章两区块：走势表（as-if 市值曲线+累计收益率+最大回撤+年化波动率+基准对比）+ 回撤矩阵（回撤面积图+独立回撤事件明细）+ 危机区间标注（2015/2018/2020/2022） |
| 14 | 组合演进 | E/B/L | 演进 | 多快照趋势追踪：总市值/总盈亏/HHI/TOP 持仓占比变迁，快照不足时占位 |
| 15 | 数据源可用性矩阵 | E/B/L | 基础报表 | 各数据源实时可用性状态汇总 |
| 16 | 持仓基本面 | E/B/L | 基础报表 | 同页签两区块：①财务指标（开关 `financial_indicator`，需数据底座就绪，20 列）②持仓个股财报摘要（开关 `financial_report_digest`，需 DataSinking key，10 列）；两开关各控一块，任一就绪即显示。两区块均含「标的来源」列区分直接持有 / 穿透自基金 |
| 17 | LLM API 用量 | L | LLM | 当前会话的 LLM API 用量汇总（强制末位） |

### 6.4 各模块字段定义

#### 6.4.1 投资分析汇总

| 字段 | 内容 |
|:-----|:------|
| 当前时间 | 生成日期时间 + 所属交易日 |
| A 股指数 | 上证指数、沪深300、创业板指（涨跌幅+点数） |
| 美股指数 | 道琼斯、纳斯达克、标普500（涨跌幅+点数） |
| 账户汇总 | 总市值、总成本、总盈亏、本日盈亏 |

#### 6.4.2 持仓明细与分类（同页签两区块）

##### 区块① 市值核算明细

15 列明细：

| # | 列名 | 说明 |
|---|------|------|
| 1 | 账户 | 所属账户名 |
| 2 | 名称 | 资产全称 |
| 3 | 代码 | 6 位代码 |
| 4 | 最新价 | 当前价格/净值 |
| 5 | 净值日期 | 具体日期 |
| 6 | 昨日价 | 前一日收盘价/净值 |
| 7 | 取价方式 | 场内实时价/场内收盘价/官方净值(T)/官方净值(T-1) |
| 8 | 溢价率 | 仅 QDII 基金显示 |
| 9 | 份额 | 持仓数量 |
| 10 | 市值 | 价格 × 份额 |
| 11 | 成本 | 成本价 × 份额 |
| 12 | 盈亏 | 市值 - 成本 |
| 13 | 收益率 | (市值-成本)/成本 |
| 14 | 本日盈亏 | 仅场内品种计算 |
| 15 | 取价渠道 | 腾讯财经/东方财富/天天基金网等 |

**格式规则**：
- 分账户小计在该账户资产下立即显示，总计在最后
- 盈亏正数红色，负数绿色
- 场内收盘价 / 官方净值(T) / QDII 官方净值(T-1) 蓝色标识

##### 区块② 持仓分类汇总

| # | 列名 | 说明 |
|---|------|------|
| 1 | 资产属性 | 股票/基金/债券/现金 |
| 2 | 投资分类 | A股/QDII/主动/被动/指数/混合/纯债/货币 |
| 3 | 名称 | 资产全称 |
| 4 | 代码 | 6 位代码 |
| 5 | 市值 | 最新价 × 份额 |
| 6 | 成本 | 成本价 × 份额 |
| 7 | 盈亏 | 累计盈亏 |
| 8 | 收益率 | 累计收益率 |
| 9 | 本日盈亏 | 仅场内品种计算 |
| 10 | 年均股息率 | 持仓 A 股的年均股息/最新价 |

#### 6.4.3 资产穿透TOP10

| # | 列名 | 说明 |
|---|------|------|
| 1 | 排名 | 1~10 |
| 2 | 名称 | 资产名称 |
| 3 | 代码 | 6 位代码 |
| 4 | 穿透市值 | 合并后的市值 |
| 5 | 占比 | 占总穿透市值比例 |
| 6 | 板块 | 所属板块/行业 |
| 7 | 概念 | 概念标签 |
| 8 | 预测EPS(YYYYE) | 机构盈利预测 |
| 9 | 年均股息 | 历史分红每股年均金额 |
| 10 | 来源明细 | 来自哪些基金的持仓 |

**板块分类**：优先使用 API 行业分类数据，API 不可用时关键词映射回退。

**基金底层持仓取数（三跳阶梯）**：季报接口（年份域，最近 4 个完整季度回溯，唯一携带真实报告期的通道）→ 基金主页面前十大（报告期不从此处取）→ 季报接口无年份兜底（最早可得报告，通常已陈旧）。取数层不做时效判定，兜底得到的陈年报告照常上送，由报告层时效闸门裁决并标注「报告期 X，距今 N 个完整季度」。**备源（同花顺官方披露持仓，需用户自备 key）**：天天基金全链不可用时由 `hithink` 接管，原始载荷经同一归一器（`fetcher/fund.py::_normalize_hold_payload`）落到同一契约——只取 `asset_type=stock` 的明细（债券/基金资产不进股票层）、报告期取自披露的结束日期、联接基金（持仓仅一只 `fund` 型目标 ETF）直接带回目标 ETF 代码供穿透链路使用；主源可用时输出逐字不变。

**R-PEN-01**（联接基金穿透，开关 `feeder_penetration` 默认开）：ETF 联接基金的资产即其目标 ETF、本身不持有股票，其季报股票表按构造为空。开启时按目标 ETF 的持仓与报告期穿透其底层资产；目标 ETF 由基金主页面锚点**动态解析**（不维护映射表，标的更换自动跟随），结构深度恒为 1。联接基金与目标 ETF 的「相关」链接互为反向（联接页标签止于「ETF」指向场内目标、常规 ETF 页标签多「联接」二字指向场外联接基金），解析以标签形态 + 场内代码两重区分，且调用前先以 `is_index_link_by_name` 过滤名称。

#### 6.4.4 基金业绩分析

| # | 列名 | 说明 |
|---|------|------|
| 1 | 基金 | 基金名称 |
| 2 | 代码 | 6 位代码 |
| 3 | 类型 | 场内ETF/场外主动型/场外指数/场外QDII/场外债券 |
| 4 | 近3月 | 近3月收益率 |
| 5 | 近6月 | 近6月收益率 |
| 6 | 近1年 | 近1年收益率 |
| 7 | 持仓累计盈亏(¥) | 市值 - 成本 |
| 8 | 持仓收益率 | 盈亏 / 成本 |
| 9 | 业绩基准 | 比较基准名称 |
| 10 | 业绩评价 | 优秀/良好/稳定/偏差/较差 |
| 11 | 同类排名 | 格式"排名/总数" |

> **业绩评价评级阈值（类型差异化）**：5 级评级由同类百分位/排名计算，阈值随基金类型差异化（债券型/QDII 放宽、指数/ETF/联接收紧、主动权益/股票走默认），类型由基金名称按「QDII → 债券 → 指数/ETF/联接 → 主动权益」优先级判定，与穿透分类一致。

| 类型 | 优秀 | 良好 | 稳定 | 偏差 | 较差 |
|------|------|------|------|------|------|
| 主动权益/股票（默认） | ≤10% | ≤30% | ≤50% | ≤75% | >75% |
| 债券型 | ≤15% | ≤35% | ≤55% | ≤80% | >80% |
| QDII/海外 | ≤15% | ≤35% | ≤55% | ≤80% | >80% |
| 指数/ETF/联接 | ≤10% | ≤25% | ≤45% | ≤70% | >70% |

> 可选**候选基金比较子表**（功能开关 `candidate_compare` **默认关**）：候选来自 `comparison_candidates`（6 位基金代码 ≤10 只），横向比较 11 列——候选基金、代码、评级、近1月/近3月/近6月/近1年、同类排名、最大回撤、风格、与持仓重合（Jaccard 系数 + 对应基金名）。单候选获取失败该行"数据获取失败"占位；开关关闭时不渲染，主表输出保持不变。

##### 基金经理变更监控块（「基金业绩分析」章末尾子视图）

> 章节层合并后本块位于「基金业绩分析」页签/章节末尾，随 `enable_fund_deep_analysis` 显隐；字段定义如下。

| # | 列名 | 说明 |
|---|------|------|
| 1 | 基金名称 | 基金全称 |
| 2 | 代码 | 6 位代码 |
| 3 | 当前基金经理 | 现任经理姓名 |
| 4 | 任职天数 | 当前经理已任职天数 |
| 5 | 1 月内变更 | 🔴 是 / ✅ 否 |
| 6 | 3 月内变更 | ⚠️ 是 / ✅ 否 |
| 7 | 6 月内变更 | ⚠️ 是 / ✅ 否 |
| 8 | 预警级别 | 🔴 紧急 / ⚠️ 关注 / 📋 首检 / ✅ 正常 |

**预警规则**：
- 30 天内发生变更 → 🔴 紧急
- 90 天内发生变更 → ⚠️ 关注
- 180 天内发生变更 → ⚠️ 关注
- 首次运行无历史快照 → 📋 首检
- 以上均不满足 → ✅ 正常

#### 6.4.5 持仓结构与集中度（同页签三区块）

##### 区块一/二 持仓重合度矩阵 + 持仓相关性矩阵

一章两区块（上区块 持仓重合度矩阵、下区块 持仓相关性矩阵），章节可见性 = 任一区块有数据。

**区块一 持仓重合度矩阵**：

**双指标**：Jaccard 系数 = |A∩B| / |A∪B|，重叠率 = |A∩B| / min(|A|,|B|)，最终取二者最大值。

**着色规则**：
| 重合度 | 着色 |
|:-------|:-----|
| ≥ 50% | 红色白字 |
| 30~50% | 橙色白字 |
| 15~30% | 黄色黑字 |
| > 0% | 绿色黑字 |
| 0% | 无着色 |

**触发条件**：持仓中基金数量 ≥ 2 只。

**区块二 持仓相关性矩阵**：

**热力图**：下三角相关矩阵（行品种 × 列品种），红=正相关 / 蓝=负相关 / 白=不显著 / 灰=N/A。

**明细表**：

| # | 列名 | 说明 |
|---|------|------|
| 1 | 序号 | 行号 |
| 2 | 品种A | 行品种 |
| 3 | 品种B | 列品种 |
| 4 | 相关系数 r | 两品种日收益率的 Pearson 相关系数 |
| 5 | p 值 | 相关性显著性 p 值 |
| 6 | 样本数 | 重叠样本数 |
| 7 | 显著性 | 95% 双尾检验（p < 0.05）为显著 |

**说明**：窗口 / 有效样本 / 显著性阈值；重叠样本不足窗口期的品种相关标为 N/A（灰色）。

##### 区块三 持仓集中度监控

| # | 列名 | 说明 |
|---|------|------|
| 1 | 基金名称 | 基金全称 |
| 2 | 代码 | 6 位代码 |
| 3 | 前 3 占比 | 前 3 大持仓占比 |
| 4 | 前 5 占比 | 前 5 大持仓占比 |
| 5 | 前 10 占比 | 前 10 大持仓占比 |
| 6 | 上期前 10 | 历史快照中的前 10 占比 |
| 7 | 环比变化 | 本次 - 上期（附 ↑↓ 箭头） |
| 8 | 预警 | 🔴 紧急 / ⚠️ 关注 / 📋 首次 / ✅ 正常 |

**预警规则**：
- 环比变化 > +20% → 🔴 紧急
- 环比变化 > +10% → ⚠️ 关注
- 当前前 10 > 80% → ⚠️ 关注
- 首次运行 → 📋 首次

#### 6.4.6 风格与因子分析

> 一章三区块（区块一 基金风格表、区块二 风格因子回归、区块三 行业 Beta），任一块数据独立降级。

**区块一 基金风格表**：

| # | 列名 | 说明 |
|---|------|------|
| 1 | 基金名称 | 基金全称 |
| 2 | 代码 | 6 位代码 |
| 3 | 当前风格 | 六宫格风格（如"大盘成长"） |
| 4 | 上期风格 | 历史快照中的风格 |
| 5 | 漂移等级 | 🔴 严重 / ⚠️ 中度 / ▲ 轻度 / 📋 基准确立中 / ✅ 无 |
| 6 | 漂移评分 | 网格曼哈顿距离（0~4） |

**风格判定标准**：
- 市值：>500亿=大盘、100~500亿=中盘、<100亿=小盘
- 估值：PE/行业平均PE <70%=价值、>130%=成长、其余=混合
- 最终风格 = 市值权重最大的 size + PE 权重最大的 style

**降级链路**：
1. push2 API（精确，优先）
2. Tencent 扩展字段（可靠，不标注估算）
3. 代码前缀估算（兜底，标注"估算风格"）

**区块二 风格因子回归**：

**主表**：

| # | 列名 | 说明 |
|---|------|------|
| 1 | 风格因子 | 价值/成长/质量（固定 MVP 展示顺序） |
| 2 | 暴露系数 β | 组合对风格因子的回归暴露 |
| 3 | t 值 | 回归系数 t 统计量 |
| 4 | 显著（95%） | 双尾 t 检验是否显著 |
| 5 | 风格归属占比 | 因子风格在组合中的归属权重 |

**基准对照**：组合 β | 基准 β | 相对暴露。

**备注**：窗口 / 有效样本 / α（截距）/ 因子相关性。数据不足或数据源故障时写入占位文本。

**区块三 行业 Beta 子表**（功能开关 `industry_beta` 开关，默认关）：

| # | 列名 | 说明 |
|---|------|------|
| 1 | 行业 | 持仓行业名（`batch_fetch_industry_data` 分类） |
| 2 | 暴露占比 | 行业持仓市值占比 |
| 3 | 指数代码 | 中证行业指数代码（`INDUSTRY_INDEX_MAP`） |
| 4 | β | 组合对行业指数的回归暴露 |
| 5 | t 值 | 回归系数 t 统计量 |
| 6 | 显著（95%） | 双尾 t 检验是否显著 |
| 7 | 相关性 r | 组合与行业指数相关性 |

行业名无对应指数时归入表尾 `unmapped_industries` 行（指数代码/β 等列显示 "—"）。开关关闭 → 区块隐藏；开关开启但数据不足/指数拉取失败 → 标题 + 占位文本。

#### 6.4.7 行动建议

**字段说明**：

| # | 子块 | 说明 |
|---|------|------|
| 1 | 行动摘要 | 行动建议概述行（引用再平衡信号条数） |
| 2 | 再平衡信号 | 单品占比超警戒线品种清单（代码/名称/占比/警戒线/建议动作） |
| 3 | 交易纪律 | 止盈/止损/回撤三类纪律提醒（收益率超线触发，输出触发/距触发幅度/建议动作） |
| 4 | 调仓建议 | 可行化调仓清单（代码/名称/操作/份额/金额/预估费用/调仓后现金） |
| 5 | 收益归因 | 品种收益贡献占比（盈利来源/亏损来源；无盈亏或无持仓时为空） |

**数据来源**：`analysis/action_advisor.build_action_data` 纯算法计算（基于持仓明细 + 总市值），零新增数据源。无持仓数据时 `available=false`，写「无持仓数据，行动建议无法生成」占位（technical.md §1.4.5 降级）。

**需求标识**：

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-ACT-01 | 行动建议由独立顶层章节承载（`action`），独立开关 `enable_action` 控制（**默认开**，菜单 P 可切换），basic/both/full 均可见 |
| R-ACT-02 | 单源计算两处呈现：`action_data` 经 orchestrator 组装进 pipeline_data（数据契约），行动建议 + 智囊团深度复盘「行动摘要」子块共享同一对象（渲染数据经context传递，无模块级全局变量） |
| R-ACT-03 | 再平衡信号基于单品占比与警戒线阈值；全部合规时信号为空但 available=True |
| R-ACT-04 | 调仓建议/收益归因已实现，无数据时写「待生成」占位文本，报告结构保持稳定 |
| R-ACT-05 | 开关关闭时行动建议不渲染、智囊团深度复盘与现状一致（无行动摘要子块） |
| R-DIS-01 | 止盈纪律：单品种收益率 ≥ 止盈线（默认 +20% 可配）→ 触发「部分止盈」建议，输出「触发 + 距触发幅度 + 建议动作」结构化信号 |
| R-DIS-02 | 止损纪律：单品种收益率 ≤ 止损线（默认 -15% 可配）→ 触发「止损/减仓」建议，输出结构同 R-DIS-01 |
| R-DIS-03 | 回撤纪律：组合当前市值相对历史峰值回撤 ≥ 回撤线（默认 10% 可配）→ 触发「减仓控回撤」建议；无峰值数据时安全跳过 |
| R-DIS-04 | 纪律信号与再平衡信号同构扩展，复用静默期机制（默认 30 天可配），同一品种静默期内不重复告警 |
| R-DIS-05 | 纪律参数（止盈线/止损线/回撤线/静默期）经 `discipline` 配置段参数化，config.json 可配 |

#### 6.4.8 财经新闻热点与持仓关联分析

**关键词富化显示**：

| 来源 | 样式 | 匹配优先级 |
|:-----|:-----|:----------|
| 持仓名称 | 蓝色标签 | 最高 |
| 穿透资产 | 紫色标签 | ↓ |
| 概念板块 | 橙色标签 | ↓ |
| 行业分类 | 灰色标签 | 最低 |

**可选 LLM 增强**：`enabled_llm.news_correlation=true` 时启用 LLM 关联度评级（高/中/低/无关）+ 情感分析（利好/利空/中性）。

#### 6.4.9 全球政经局势

LLM 生成章节：基于指数行情与持仓结构，输出全球政经局势分析。内容为叙述性分析文本，随模块状态（成功/降级/失败）降级或占位。

#### 6.4.10 智囊团深度复盘

LLM 生成章节：三阶段圆桌会议式深度复盘。启用辩论模式 Feature Flag（正反辩论/条件推理/集中度问答）时，输出含辩论内容并标注"🧪 辩论模式"/"🧪 实验模式"标签。

#### 6.4.11 持仓体检报告
LLM 五维度量化评分，每项满分 100：

| # | 维度 | 评估要点 |
|---|------|---------|
| 1 | 风险分散度 | 行业集中度、单品种集中度、穿透资产集中度 |
| 2 | 流动性 | 场内/场外比例、停牌风险、基金封闭期 |
| 3 | 收益合理性 | 盈亏是否合理、与大盘/同类对比 |
| 4 | 成本结构 | 成本分布、浮盈浮亏比 |
| 5 | 数据质量 | 输入数据完整性（收盘价断点/降级频次/净值延迟/分红状态/缺失时长） |

输出综合评分（总分/100）+ 评级（优/良/中/差）及每维度评分依据/扣分项。

#### 6.4.12 穿透深度分析
**输出内容**：

| # | 字段 | 说明 |
|---|------|------|
| 1 | 行业集中度 | 穿透后行业分布与集中度 |
| 2 | 国别暴露 | 穿透资产的国家/地区暴露 |
| 3 | 穿透 TOP 明细 | 底层标的拆解合并排序（含盈利预测 EPS、年均股息列，数据源失败时显示 --） |

数据不足或数据源故障时按数据降级治理写入占位。

#### 6.4.13 组合历史走势与回撤（一章两区块 + 危机区间标注）
**输出指标（走势表区块）**：

| 字段 | 说明 |
|:-----|:------|
| 折线图 | as-if 市值曲线（当前持仓份额 × 历史价格/净值，全程集日期） |
| 累计收益率 | 期末-期初 / 期初。起算点从 ≥80% 持仓覆盖的日期开始 |
| 最大回撤 | Peak-to-Trough 算法，返回金额+百分比+起止日期 |
| 年化波动率 | 日收益率样本标准差（ddof=1）× √252 |
| 基准指数对比 | 各基准归一化至 100 基点走势叠加、区间累计收益率、区间最大回撤 |
| 指标汇总矩阵 | 累计收益率/累计收益/最大回撤/年化波动率/起算日/终止日（组合 vs 基准，仅一份） |

**输出指标（回撤矩阵区块）**：独立水下事件扫描——序号/起峰日/最深日/恢复日/最大回撤/持续天数/恢复耗时/当前状态（已恢复/未恢复），有效交易日 < MIN_SPAN 时显示数据不足提示。

**输出指标（危机区间标注区块）**：预设历史危机区间（2015 股灾 / 2018 贸易摩擦 / 2020 疫情 / 2022 调整，静态历史事实表，不随持仓变化、不拉长 lookback、零新增网络请求）与报告数据窗口重叠裁剪后统计：区间最大回撤/最深日/恢复耗时/是否已恢复；无重叠时显式写「报告数据窗口内无历史危机区间」占位。

**输出指标（尾部风险统计区块）**：基于 `history_data.bars` 历史日收益序列（与走势表同口径 (curr-prev)/prev）计算尾部风险指标——VaR(95)/VaR(99)（历史模拟法，单日最大可能损失百分比）、最大单日跌幅（% + 发生日期）、最长连续下跌天数（含区间起止日期）、最大单日跌幅后恢复天数（已恢复 / 未恢复 / 无下跌日三种状态）；日收益样本 < MIN_SAMPLE=20 时 available=false，写「样本不足」占位（technical.md §1.4.5 降级）。

**渲染**：
- HTML：交互净值折线图（Chart.js，含基准指数叠加 + 危机区间阴影带着色，**必须 图下说明**）+ 回撤面积图 + 指标汇总卡片 + 回撤明细表 + 危机区间表 + 尾部风险卡（VaR95/VaR99/最大单日跌幅/最长连续下跌/最大跌幅后恢复，**图下说明**注明历史模拟法）
- Excel：`portfolio_history_drawdown` 页签分「走势表 + 回撤矩阵 + 危机区间标注」三区块，每基准一列（归一化值 0.00 格式），回撤矩阵为独立回撤事件明细（含恢复耗时）。尾部风险统计写入 `_write_tail_risk_rows` 五行（VaR95/VaR99/最大单日跌幅/最长连续下跌/恢复天数，百分比按 FMT_PERCENT 存小数）

**数据覆盖**：
- 不同持仓的历史起止日期可能不同（如 QDII 可追溯较远、债券基金较短）
- 收益率计算从 ≥80% 持仓覆盖的日期起算
- 早期数据保留在走势图上但排除出收益率计算
- 危机区间标注基于 `history_data.bars` 派生（`crisis_annotation_data` 数据契约），数据不可用（status=unavailable / bars 空）时危机区块落占位
- 尾部风险统计基于 `history_data.bars` 派生（`tail_risk_data` 数据契约，`analysis/tail_risk.py::compute_tail_risk`，纯标准库、analysis 层隔离），样本不足时 available=false 落占位

**需求标识**：

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-TAIL-01 | 尾部风险指标复用 `history_data.bars` 历史日收益序列计算，不额外拉长 lookback、无新增网络请求；与走势表日收益同口径 |
| R-TAIL-02 | 输出 VaR(95/99)（历史模拟法）、最大单日跌幅、最长连续下跌、最大跌幅后恢复天数；恢复状态分已恢复/未恢复/无下跌日三态 |
| R-TAIL-03 | 日收益样本 < MIN_SAMPLE=20 时 `tail_risk_data.available=false`、各指标 None，Excel/HTML 均写「样本不足」占位（technical.md §1.4.5 降级） |
| R-TAIL-04 | 尾部风险数据注入 `tail_risk_data` 键，Excel 尾部指标行 + HTML 尾部风险卡双端消费，HTML 卡下附 图下说明（历史模拟法 VaR） |

#### 6.4.14 组合演进（多快照趋势）
**字段说明**：

| # | 字段 | 说明 |
|---|------|------|
| 1 | 汇总 | 快照总数、有效观察日数、周期起止 |
| 2 | 总市值与总盈亏趋势 | 各期总市值 / 总成本 / 总盈亏 / 持仓品种数（表 + 折线图） |
| 3 | HHI 趋势 | 各期持仓集中度 HHI（有有效权重才记录，否则 "-"） |
| 4 | TOP 持仓占比变迁 | 各期 TOP 持仓的权重占比变迁 + 出现期数（仅多品种时渲染图） |
| 5 | 账户配置流 | 多账户时各账户占比随时间的流式变化 |
| 6 | 说明 | 权重口径（快照缺市值时回退成本）、快照格式限制 |

**数据来源**：`data/history/snapshots/snapshot_{timestamp}.json` 多期快照本地聚合，零网络请求。有效快照 < 3 时 `available=false`，写「组合演进数据不足」占位。权重口径：历史快照 `market_value=0.0` 时回退成本（`份额 × 每份成本`）。

**需求标识**：

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-EVO-01 | 组合演进基于本地快照聚合，零网络请求；快照数不足时降级占位（可用性由 `evolution_data.available` 控制） |
| R-EVO-02 | 历史快照缺市值时权重回退成本口径，占比数据口径在说明区注明 |
| R-EVO-03 | 演进数据注入 `evolution_data` 键，Excel「组合演进」页签与 HTML「组合演进」章节双端消费 |
| R-EVO-04 | 组合演进章节可见性由独立配置项 `enable_portfolio_evolution` 控制（默认开启），与 `enable_history`/`enable_fund_deep_analysis` 相互独立；关闭仅隐藏报告章节，不影响持仓快照自动记录 |

**自上次快照变化摘要（组合演进章顶部区块）**：对比去重后最近两次快照，输出新增/移除品种（按 code 合并比对，复用 `fetcher/history_diff.HistoryDiff` 引擎）、集中度 HHI 变化（本期 - 上期，权重口径与演进一致：市值优先、市值为 0 回退成本）、超 15% 警戒线品种（阈值复用 `analysis/simple_rebalance._THRESHOLD`，按权重降序），生成一段中文变化摘要文本供双端直接渲染。数据来源复用 `data/history/snapshots/` 本地快照，零网络请求。去重后有效快照 < 2 期（无上次快照可对比）时 `available=false`，写「快照差异不足」占位（technical.md §1.4.5 降级）。由 `analysis/snapshot_diff.py::build_snapshot_diff()` 计算（纯标准库、analysis 层隔离，复用 `portfolio_evolution` 的 `_dedup_by_date`/`_compute_hhi`/`_holding_weight`），注入 `snapshot_diff_data` 键。

**需求标识**：

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-DIFF-01 | 自上次快照变化摘要基于本地快照对比，零网络请求；复用既有多快照数据（`data/history/snapshots/`），与组合演进同数据源 |
| R-DIFF-02 | 输出新增/移除品种、集中度 HHI 变化（本期-上期，权重口径与演进一致）、超 15% 警戒线品种（按权重降序）；HHI 无可比权重时跳过 HHI 变化点 |
| R-DIFF-03 | 去重后有效快照 < 2 期时 `snapshot_diff_data.available=false`、reason 说明，Excel/HTML 均写「快照差异不足」占位（technical.md §1.4.5 降级），不阻断报告生成 |
| R-DIFF-04 | 快照差异摘要注入 `snapshot_diff_data` 键，Excel 组合演进页签顶部「自上次快照变化摘要」区块 + HTML 组合演进章顶部摘要卡双端消费，与演进数据同开关（`enable_portfolio_evolution`） |

#### 6.4.15 数据源可用性矩阵
**字段说明**：

| # | 字段 | 说明 |
|---|------|------|
| 1 | 数据源类别 | 按功能分类（行情/基金排名/基金持仓/行业分类/指数/盈利预测/分红/资金流向） |
| 2 | 状态 | 综合健康度：✅ 正常 / ⚠ 降级 / ❌ 失败 |
| 3 | 详情 | 各计数汇总，如"3 正常，1 降级" |
| 4 | 正常 | 该类别下运行正常的数据源数量 |
| 5 | 降级/失败 | 该类别下降级和失败的数据源数量 |

**状态判定规则**：
- **ok**：该类别下全部数据源正常（失败=0 且 降级=0）
- **failed**：该类别下全部数据源失败（失败>0 且 正常=0）
- **degraded**：其他情况（部分正常、部分降级/失败）

数据由 DegradationTracker 事件日志实时聚合，在报告生成时自动采集各数据源最新状态。失败项在表格下方展示具体失败源明细。

#### 6.4.16 持仓基本面（同页签两区块）

| # | 区块 | 说明 |
|---|------|------|
| 1 | 区块① 财务指标 | 字段定义与规则见 §5.9（R-FIN-*）与 §6.4 转载说明；Excel 20 列（名称/代码/**标的来源**/报告期/文种/营收/归母净利/营收同比/净利同比/毛利率/ROE/负债率/经营现金流/每股收益/每股净资产/PE/PB/质量档/年度趋势/来源） |
| 2 | 区块② 持仓个股财报摘要 | 字段定义与规则见 §6.12（R-FRD-*）；Excel 10 列（名称/代码/**标的来源**/报告期/文种/标题/披露日/摘要/来源/原文链接） |

> 两区块各由自己的功能开关控制（`financial_indicator` / `financial_report_digest`，均默认关）：开关关闭 → 该块整体不渲染；开启但无数据 → 该块写占位。章节整体可见性按两契约 OR（任一就绪即显示）。

#### 6.4.17 LLM API 用量
**汇总区**：API 调用次数、模型、Endpoint、输入 Token、输出 Token、总 Token、缓存命中 Token、累计费用。

**各模块明细表**：

| # | 列名 | 说明 |
|---|------|------|
| 1 | 模块 | LLM 模块名称 |
| 2 | 状态 | 成功/缓存/失败/禁用（着色标识） |
| 3 | 模型 | 实际调用模型 |
| 4 | 总 Token 用量 | 输入 + 输出合计 |
| 5 | 输入 Token | 输入 token 数 |
| 6 | 输出 Token | 输出 token 数 |
| 7 | 缓存命中 Token | 命中缓存 token 数 |
| 8 | 费用 | 模块调用费用 |
| 9 | LLM 缓存 | 缓存命中状态 |
| 10 | Thinking | 思考链 token 用量 |

强制末位页签。实验模式（辩论）启用时在 expert_review 模块与汇总区标注实验模式标识。

#### 6.4.18 成本流水（功能开关 `cost_lots` 子模块，默认关）
功能开关 `cost_lots` 开启后，汇总/市值/分类页签渲染成本分档 + 资金加权收益率（XIRR）+ 分红累计：

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-CFL-01 | 持仓 Excel 含「交易流水」页签（日期/代码/操作/份额/价格，可选费用列）与「分红流水」页签（日期/代码/每份分红）时，按真实流水精确计算：XIRR 资金加权收益率（买入/申购为负现金流、卖出/赎回与分红到账为正、期末市值为资金收回）、成本分档（交易流水按代码 FIFO 合并批次，相对当前市价分低成本/高成本/未分档）、分红累计（每份分红 × 登记日份额，份额未知回退当前持仓份额） |
| R-CFL-02 | 无流水页签时自动切换为**快照近似**（`approximate=true`）：按单笔建仓假设（建仓日 = `holdings_start_date`，未配置则仅成本分档近似）合成流水复用同一契约，渲染层写「⚙ 成本流水为可选进阶增强：当前未录入交易/分红流水，已用持仓快照近似计算」说明，替代「必须录入流水」的压力文案 |
| R-CFL-03 | 开关关闭时返回 `None`，各页签保持既有输出（不渲染成本流水相关列/行） |
| R-CFL-04 | 分类表追加「成本分档」「分红累计」子列；XIRR 在汇总区以一行展示（真实流水无标注，快照近似标注「近似」） |

### 6.5 取价方式规范

| 取价方式 | 触发条件 |
|:---------|:---------|
| 场内实时价 | 腾讯 source_api 且市场开盘 |
| 场内收盘价(T) | 腾讯 source_api，盘后，nav_date == 当日 |
| 官方净值(T) | East Money source 且 nav_date == 当日 |
| QDII 官方净值(T-1) | QDII 基金，nav_date == 前一交易日 |
| 官方净值(T-N) | nav_date 为 2~5 个交易日前 |

### 6.6 持仓快照数据需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-SNP-01 | 每次生成 B/L 报告时自动创建持仓快照，用于环比对比 |
| R-SNP-02 | 快照存放独立于缓存系统，不受菜单缓存清理命令影响 |
| R-SNP-03 | 快照保留天数可配置（默认 60 天），超出限制自动删除最旧文件 |
| R-SNP-04 | 快照最大数量可配置（默认 365 个），超出限制自动删除最旧文件 |
| R-SNP-05 | 快照对比输出：总市值变化、总盈亏变化、新增/清仓/增持/减持品种 TOP5 |
| R-SNP-06 | 首次运行无历史快照时跳过对比，下次自动建立基线 |

### 5.11 景气度框架诊断（实验性开关 `prosperity_framework`）

> 方法骨架借鉴开源项目 zhengxi-views（郑希观点库，MIT）：只借鉴其可计算骨架与六维评分卡口径，不引入其语料/基金快照/检索。设计见 `docs-stm/plan/prosperity-framework-design.md`。

| # | 需求 | 说明 |
|---|------|------|
| R-PF-01 | **实验性开关**：`prosperity_framework` 默认关（`GROUP_EXPERIMENTAL`、`affects_report=True`）；关闭时报告与未引入时**逐字节一致**（零行为变化） |
| R-PF-02 | **六维评分卡**：景气方向/通胀属性 25 + ROE 低位弹性 20 + 全球视野/中国比较优势 15 + 流动性 10 + 集中度与周期拼接 15 + 业绩与回撤印证 15 = 满分 100 |
| R-PF-03 | **客观化口径**：全部输入取自既有能力（穿透重仓的板块/概念、`financial_indicator_data` 的 ROE、`check_liquidity` 的变现天数、历史快照的换手代理、`history_data` 的区间收益与最大回撤）；不新增外部数据源、不新增 LLM 调用 |
| R-PF-04 | **数据缺失不臆造**：维度数据缺失一律标 `unverified`，**不计分**并给出可读原因；总分只按已计分维度折算（`scored_weight` / `total_score_pct`），界面同时显示未验证清单 |
| R-PF-05 | **评级口径**：≥80 高度契合 / 60–79 较契合 / 40–59 部分契合 / <40 不契合；渲染固定带免责句（衡量「组合与框架的契合度」，非组合优劣，非投资建议） |
| R-PF-06 | **呈现位置**：行动建议章内嵌块（HTML ⑥ 块 + Excel `_write_prosperity_block`），不新增报告章节、不改注册表条目与序号 |
| R-PF-07 | **可核对**：每条维度得分在 `evidence` 中给出可追溯口径（含占比/天数/收益率等数值），持仓视角清单按权重降序列出板块与 ROE 备注 |
| R-PF-08 | **配置**：顶层键 `prosperity_framework`（`boom_keywords` / `global_edge_keywords` / `defensive_keywords` / `concentration_target_pct`，手动编辑）；关键词与目标值可随投资框架演进调整 |

### 6.7 流动性风险分析

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LIQ-01 | 系统应计算场内品种（股票/ETF）的变现天数，计算公式：市值 / 近 20 日日均成交额 |
| R-LIQ-02 | 场外基金应支持用户配置单日赎回上限（`redemption_limits`），计算全量赎回所需天数 |
| R-LIQ-03 | 未配置赎回上限的场外品种应标记"需手动确认赎回上限"，不阻塞报告生成 |
| R-LIQ-04 | 成交额数据不可用（K 线缺失/API 降级）时默认假设流动性充足，不告警 |
| R-LIQ-05 | 流动性分析结果应注入智囊团深度复盘（`expert_review`）LLM prompt 的流动性小节 |
| R-LIQ-06 | 场内品种按变现天数分级：当日可卖出（<1日）/需多日卖出（≥1日） |
| R-LIQ-07 | OTC 品种（债券/货币基金）标记为 otc 类型，不进行场内流动性计算 |
| R-LIQ-08 | 香港/美股等非 A 股品种标记为 assumed_liquid，不进行场内流动性计算 |

### 6.8 再平衡信号

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-RBL-01 | 系统应计算各品种权重 = 市值 / 总市值，超限阈值默认 15% 可配置（`rebalance.threshold`） |
| R-RBL-02 | 系统应支持大类配置（权益/固收）偏离度计算，偏离阈值默认 5% 可配置（`rebalance.deviation_threshold`） |
| R-RBL-03 | 系统应提供三套预设阈值（conservative 10%/3%、moderate 15%/5%、aggressive 25%/8%），通过 `rebalance.profile` 切换 |
| R-RBL-04 | 同一品种触发再平衡信号后应有静默期（默认 30 天可配），静默期内不重复告警 |
| R-RBL-05 | 静默期状态应跨会话持久化到 `data/state/rebalance_silence.json` |
| R-RBL-06 | 再平衡信号应附带置信度评估（high / medium / low），基于偏离幅度、持续时间和数据质量决定 |
| R-RBL-07 | 系统应提供三类误报防护：(1) 分红/拆股跳变排除，(2) 新买入品种不足 20 个**交易日**不触发（持仓期按交易流水首次买入日起以交易日计，无流水时不做该项过滤），(3) 临近到期品种标注剩余期限 |
| R-RBL-08 | 再平衡结果应注入智囊团深度复盘 LLM prompt 的调仓建议小节 |

### 6.9 竞争语境（基准对比）

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-CTX-01 | 系统应支持多指数对比，通过 `comparison_indices` 配置基准指数池（默认：沪深300+中证500+中证全债） |
| R-CTX-02 | 竞争语境应展示组合与各基准的收益对比（当日涨跌幅对比 + 历史走势区间累计收益率对比） |
| R-CTX-03 | 竞争语境应展示组合与各基准的风险指标对比（夏普比率、年化波动率、最大回撤） |
| R-CTX-04 | 竞争语境段落应附口径说明脚注：费后净收益 vs 价格指数、含现金管理品种 vs 不含、持仓变动非静态组合 |
| R-CTX-05 | 竞争语境段落应附加幸存者偏差提示，说明对比指数成分股定期调整效应 |
| R-CTX-06 | LLM 仅做数据陈述（"组合收益 X%，指数收益 Y%，差异 Z 个百分点"），不做"跑赢/跑输"的明确结论性判断 |
| R-CTX-07 | 自定义基金池可通过 TUI 菜单（选项 I）管理，预设默认池无需用户手动维护 |

### 6.10 汇率敞口分析

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-FX-01 | 根据持仓品种的代码（港股通 5 位纯数字）和名称关键词（QDII/海外基金）自动判定交易币种：CNY（人民币）/ HKD（港币）/ USD（美元） |
| R-FX-02 | 以持仓市值为权重汇总各币种占比（百分比 + 金额），支持多币种混合持仓 |
| R-FX-03 | 港股通品种标注"港币计价，实际换汇成本隐含在汇率中"说明，避免误解为人民币直接交易 |
| R-FX-04 | 非人民币资产合计占比 > 0% 时附加汇率波动风险提示 |
| R-FX-05 | 汇率敞口分析结果应注入智囊团深度复盘（`expert_review`）LLM prompt 的【币种敞口分布】段落 |

### 6.11 调仓 What-if 模拟

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-WIF-01 | 提供 `whatif` CLI 子命令，对比两份持仓文件（基准 base / 目标 candidate），生成调仓 diff 报告；`--candidate` 必填，`--base` 可选（缺省用 config 配置的持仓文件） |
| R-WIF-02 | 变动类型识别：新增 / 清仓 / 加仓 / 减仓 / 不变（份额差 < 1e-3 视为不变），统计各类数量 |
| R-WIF-03 | 成本口径截面比较（权重 = 成本 ÷ 总成本，成本 = 份额 × 每份成本）：汇总指标（总成本/总份额/品种数/HHI）基准 vs 目标 + 变化箭头 |
| R-WIF-04 | 资产大类配置对比（复用持仓分类逻辑）：基准/目标成本权重 % + 变化（百分点） |
| R-WIF-05 | 输出双产物：Excel 调仓模拟工作簿（调仓摘要 / 分类配置对比 / 持仓变动明细 3 页签，变动行底色标注）+ HTML 双栏对比页（含基准/目标资产配置环形图，每图带图下说明） |
| R-WIF-06 | 默认（不指定生效日）仅做成本口径截面比较，零网络请求；指定生效日时为 opt-in 联网时序回测（见 R-WIF-09~11），口径与局限在报告说明区注明 |
| R-WIF-07 | 两侧持仓均为空时 `available=false`，报告写「调仓对比数据暂不可用」占位；CLI 层对空持仓文件直接报错返回严重退出码 |
| R-WIF-08 | 提供 TUI 菜单入口（快捷键 W）：交互式选择基准持仓（调仓前，默认配置持仓文件）与目标持仓（调仓后/假设，持仓目录中基准之外的文件），复用同一套计算与输出逻辑生成独立报告；可选输入生效日（回车跳过）触发时序回测 |
| R-WIF-09 | `--effective-date YYYY-MM-DD` 可选参数（opt-in 联网，TUI 菜单 W 同步提供生效日输入提示）：指定生效日后，获取生效日之后的行情历史（复用既有历史数据链路），按 as-if 市值（份额 × 每日价格）计算基准/目标持仓的归一化净值曲线；生效日无效/早于当前日期时回测不可用，不阻塞主报告 |
| R-WIF-10 | 时序回测输出 5 项对比指标：区间收益 / 年化收益 / 年化波动率 / 夏普比率 / 最大回撤（基准 vs 目标 + 变化箭头），同时输出归一化净值曲线与回撤曲线序列 |
| R-WIF-11 | 回测结果在 Excel 第 4 页签「时序回测」与 HTML 时序回测章节呈现；历史数据不足/获取失败时降级占位并注明原因；回测为假设推演，不构成收益承诺 |

### 6.12 持仓基本面（财务指标 + 持仓个股财报摘要，一章两区块）

> 章节层合并：原「财务指标」（§5.9）与「持仓个股财报摘要」两章合并为**同页签/同章节两区块**——区块① 财务指标（开关 `financial_indicator`）、区块② 财报摘要（开关 `financial_report_digest`）；块级开关各控各的渲染，章节整体按两契约 OR 显示。以下 R-FRD-* 规则对应区块②；区块① 规则见 §5.9。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-FRD-01 | 「持仓基本面」章节的**区块② 持仓个股财报摘要**，功能开关 `financial_report_digest` 默认关；仅覆盖 A 股**个股**（SSE/SZSE/BSE），按沪深京代码前缀映射 FMP 风格符号。持仓中的**场外基金**（`00` 前缀与深市股票代码重叠区，如场外基金 `002943` 与深市股票 `002943.SZ`）不取个股财报，判定复用 `is_otc_fund_by_name`（名称 + 代码双维度）——避免基金名配到同名股票财报 |
| R-FRD-02 | 数据源为 DataSinking 全文本财报，需用户自备 API key；key 存通用密钥文件 `data/config/data_key.json` 以 provider 名为节的 `api_key` 字段（如 `{"datasink": {"api_key": "..."}}`），环境变量 `DATASINK_API_KEY` 可覆盖文件；凭据值永不落日志/报告/缓存 |
| R-FRD-03 | 对持仓 + 穿透中的 A 股个股标的（穿透标的带中文名与来源基金，逐行标注「穿透：来源基金」；持仓标注「直接持有」），取**最新报告期**（**跨文种**：半年报/季报通常比年报新，故一次索引请求取最近若干篇后按报告期倒序取最新一篇；`datasink.doc_types` 非空时作文种白名单，默认为空=不限）的正文摘要：先取该文档**实际章节名清单**（`/documents/{id}/sections`，带缓存），再按 `datasink.sections` 偏好顺序做**子串匹配**取精确章节名（默认偏好：管理层讨论与分析 → 经营情况讨论与分析 → 主要财务数据 → 主要会计数据 → 董事会报告，季报自动落到财务数据章节；清单不可得时回退偏好名直取），命中者拼接后按 `datasink.max_chars` 截断。首选项章节不可用（404）时**继续试下一候选**，不据此判定整只标的「未取到财报」。**章节清单非空但残缺**（DataSinking 解析异常）同样回退偏好名直取；**最新一篇报告确无目标章节时按报告期向前回溯**（最多 3 篇，命中即止；**年报/半年报优先于季报**——季报无「管理层讨论与分析」，仅作最后兜底，且标题含「公告」的信息披露条目（与财报混排在索引里）直接跳过）；**章节阶全失败时进全文阶**：整篇正文下载后按偏好关键词定位片段（跳过目录点线行，取关键词起 `datasink.max_chars` 字；最多 2 篇）——银行股等源侧未解析章节的标的据此取到年报「董事会报告」段，记录以 `section_source` 标记取用方式（sections / fulltext 两阶）；取到的那份报告的**报告期与文种如实写入**（不回填、不伪装成最新一期） |
| R-FRD-04 | 免费 key 按套餐限速：请求间隔 = 1/每秒上限（free=3、yearly=31）且逐请求生效；日配额护栏（free=8191 篇/日，yearly=131071）超限即停并告警 |
| R-FRD-05 | 缺凭据 / 无 A 股标的 / 全部无覆盖时章节写占位并给出可读指引，不阻断报告主链路；单标的失败进失败清单、不静默消失，失败原因细分（索引无该标的报告 / 目标章节缺失并附已试报告期），不再统一显示「未取到财报」 |
| R-FRD-06 | 合规：每行标注披露平台归属（`source`），再分发时保留；摘要附原文链接 |
| R-FRD-07 | 缓存分级：报告元数据两周、章节正文一月；分别以 `report_datasink_index_` / `report_datasink_doc_` 前缀管理 |

---

## 7. LLM 智能分析

### 7.1 概述

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-01 | LLM 分析是可选增强内容，仅在菜单 L 中触发 |
| R-LLM-02 | 每个 LLM 模块可通过配置独立启停（enabled_llm.x） |
| R-LLM-03 | 支持 Claude / OpenAI / Gemini 三种 Provider（DeepSeek 支持 Claude 兼容端点与 OpenAI 兼容端点两种接入），可通过 Multi-Provider Chain 配置多个备选 Provider 按策略自动切换 |
| R-LLM-04 | Provider 不可用时自动按策略递补下一备选 Provider，全链失败时降级占位文本 |
| R-LLM-05 | 所有 LLM 模块的 API 调用量（Token、费用、模块明细）需在报告中统计展示 |
| R-LLM-06 | Multi-Provider Chain 支持 4 种切换策略：priority（优先级排序）、weighted（加权随机）、cost_first（价格最低优先）、fallback_only（仅主 provider 失败时切换，等价于 priority）。`proxy_preferred` 为 per-provider 后处理标记，不属策略 |
| R-LLM-07 | 凭据（api_key）**只**存放于 `llm_key.json` 的凭据块，Provider 路由配置（`llm_providers.json`）必须经 `credentials_ref` 引用，不得内联 `api_key`——出现非空内联值时该条目校验不通过并被跳过并给出可执行修正提示；`model`/`endpoint` 作为非敏感路由字段可写在路由条目中并按条目优先覆盖凭据块同名值，条目缺省时取值于凭据块 |
| R-LLM-09 | **确定性信号预消化（含叙事-数字背离）**：`signal_pre_digest`（默认开）把算法已算出的确定性结论预消化为带方向标注的 `信号：…` 行注入专家复盘与持仓体检提示词——五路信号：市场温度、持仓估值分位、尾部风险、**持仓基本面**（质量档与年度趋势同向才给方向）、**叙事与数字背离**（管理层讨论与分析摘要语气 × 指标趋势/同比方向，确定性比对，只列依据不下结论）。出现背离项时追加一行要求：请在结论中显式指出背离点并说明以哪一侧为准；**无背离时提示词与未引入该项时逐字节一致**。基本面与背离两路受 DataSinking 数据底座门禁控制（未就绪 → 数据契约缺席 → 信号自动缺席）。**不新增 LLM 调用**：读侧注入既有契约，信号文本进缓存指纹保证读写键同源 |
| R-LLM-08 | 各 Provider 的失败原因（API 错误 / 超时 / 网络异常等）需追踪记录，在报告中按模块展示失败明细 |

### 7.2 全球政经局势

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-GM-01 | 基于 A 股指数 + 美股指数 + 持仓总市值/盈亏 + 品种分类 + 行业资金流向生成分析 |
| R-LLM-GM-02 | 输出 500 字以内，覆盖主要经济体政策走向、地缘风险、对持仓潜在影响 |
| R-LLM-GM-03 | 缓存有效期 24 小时，指数或持仓汇总变化时自动失效 |
| R-LLM-GM-04 | 支持精简模式（output_brief，≤300 字、通常 200 字左右） |

### 7.3 智囊团深度复盘

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-ER-01 | 基于持仓明细+盈亏数据，模拟三阶段圆桌会议：召集令→辩论→定音锤 |
| R-LLM-ER-02 | 召集令：指出组合核心矛盾，挑 5 位流派对立专家并标明立场 |
| R-LLM-ER-03 | 圆桌会：两轮辩论（第一轮方向、第二轮互相反驳聚焦调仓优先级） |
| R-LLM-ER-04 | 定音锤：量化调仓方案和风险提示（不可调穿透层资产） |
| R-LLM-ER-05 | 每个论点必须引用具体品种代码和收益率，禁止虚构数据 |
| R-LLM-ER-06 | 缓存有效期 2 小时，仅品种/份额/成本变化时失效（排除行情波动） |
| R-LLM-ER-07 | 支持结合持仓快照对比数据（pipeline_data）做环比分析 |
| R-LLM-ER-08 | 建议开启 Extended Thinking 深度推理 |

### 7.4 持仓体检报告

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-HC-01 | 从五个维度量化评分（每项满分 100）：风险分散度、流动性、收益合理性、成本结构、数据质量 |
| R-LLM-HC-02 | 输出综合评分和评级（优/良/中/差）及 3-5 条具体可操作建议 |
| R-LLM-HC-03 | 缓存有效期 24 小时，仅品种/份额/成本变化时失效 |
| R-LLM-HC-04 | 支持结合持仓快照对比数据做环比分析 |

### 7.5 穿透深度分析

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-PD-01 | 基于穿透 TOP10 + 行业分类数据，分析行业集中度、品种集中度、国别/币种暴露 |
| R-LLM-PD-02 | 当某行业占比 > 30% 时标注集中度风险 |
| R-LLM-PD-03 | 当单品种占比 > 15% 时标注风险 |
| R-LLM-PD-04 | 缓存有效期 24 小时，穿透数据或品种变化时失效 |

### 7.6 新闻 LLM 二次关联分析

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-NC-01 | 对已排序新闻逐批进行 LLM 深度关联评分，每批最多 10 条 |
| R-LLM-NC-02 | 每条新闻输出关联度评级（高/中/低/无关）+ 情感分析（利好/利空/中性）+ 原因分析 |
| R-LLM-NC-03 | 逐条独立缓存，缓存有效期 1 小时 |

### 7.7 LLM API 用量统计

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-US-01 | 汇总区显示：调用次数、模型名称、输入/输出/总 Token、缓存命中 Token、累计费用 |
| R-LLM-US-02 | 模块明细区每模块一行：状态（成功/缓存/失败/禁用）、模型、Token、费用 |
| R-LLM-US-03 | 缓存命中计入统计但标注"缓存"来源，不计入 API 调用次数 |
| R-LLM-US-04 | 无任何用量数据时整块不渲染 |
| R-LLM-US-05 | 费用按模型定价表实时估算，支持用户自定义定价 |
| R-LLM-US-06 | 每份报告设输入 Token 预算上限（默认 8K），超出时告警而非截断，为后续模型分层提供基线数据 |
| R-LLM-US-07 | 每次 API 调用记录耗时（秒），模块明细展示各模块累计调用耗时 |

### 7.8 辩论模式（智囊团增强）

辩论模式是智囊团深度复盘（`expert_review`）模块的可选增强通路，通过 Feature Flag 独立开关，默认关闭（opt-in）。

#### 7.8.1 概述

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-DB-01 | 辩论模式是智囊团深度复盘（`expert_review`）模块的可选增强通路，通过 Feature Flag 独立开关，默认关闭 |
| R-LLM-DB-02 | 辩论模式支持三种增强形态：正反辩论（白脸/黑脸/综合）、条件推理、集中度问答 |
| R-LLM-DB-03 | 辩论模式启用时，智囊团深度复盘的生成入口由标准模式切换为 debate 通路，输出内容包含辩论标识和分段数据 |
| R-LLM-DB-04 | 辩论模式与标准模式互斥，同一报告周期内仅使用生效的一路（辩论优先于标准模式） |
| R-LLM-DB-05 | 辩论模式使用独立于标准 expert_review 的三段缓存（pro/con/synthesis），指纹复用 expert_review 的持仓指纹（排除行情波动字段），默认 TTL 24h |
| R-LLM-DB-06 | 三段独立缓存前缀注册到 registry：`llm_debate_pro_`/`llm_debate_con_`/`llm_debate_synthesis_`，分组为 preload，受菜单 [2] 刷新影响 |
| R-LLM-DB-07 | 辩论模式输出在报告页签标题尾部附加"🧪 辩论模式"/"🧪 实验模式"标签，标识当前为辩论模式产物 |

#### 7.8.2 正反辩论（白脸/黑脸/综合）

启用后，智囊团深度复盘生成过程分解为三个阶段独立调用 LLM API，每阶段独立的 system prompt 和 temperature 控制。

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-DB-PROCON-01 | 正反辩论启用的标志是 Feature Flag `llm_debate_procon` 为 true |
| R-LLM-DB-PROCON-02 | 白脸（pro）阶段：基于持仓数据生成正面论据（看多理由、持有信心、潜在收益机会），使用乐观引导型 system prompt |
| R-LLM-DB-PROCON-03 | 黑脸（con）阶段：基于持仓数据生成反面论据（风险提示、看空理由、潜在损失风险），使用审慎质疑型 system prompt |
| R-LLM-DB-PROCON-04 | 综合（synthesis）阶段：整合白脸和黑脸的论据，生成平衡的综合判断和调仓建议，使用中性综合型 system prompt |
| R-LLM-DB-PROCON-05 | 三段调用依次执行（pro→con→synthesis），每段结果独立缓存，后续直接使用缓存结果跳过 API 调用 |
| R-LLM-DB-PROCON-06 | Token 预算守卫：基于字符数计量保护 token 消耗。实际中文+Markdown 输出约 1 字符 ≈ 1 token，pro+con 累计字符超过 `int(max_total_tokens_per_report)`（默认 72000）时触发 1× 超限→跳过 synthesis 阶段（返回 pro+con 拼接）；超过 2× 超限时跳过全部 debate（回退标准模式）。配合 `per_call_max_tokens` 单段输出上限（默认 18432，经 `max_tokens_override` 生效）双闸保护，预算覆盖三段式真实成本避免 synthesis 频繁被砍 |
| R-LLM-DB-PROCON-07 | Fallback 策略：pro 或 con 阶段失败→回退标准模式输出；synthesis 阶段失败→返回 pro+con 拼接结果，日志记录 WARNING |
| R-LLM-DB-PROCON-08 | 虚构代码过滤：`_filter_hallucinated_codes()` 基于正则的行级过滤，使用 `(?:^\|[^A-Za-z0-9])([A-Za-z0-9]{4,6})(?=[^A-Za-z0-9]\|$)` 适配中文环境（替代英文 `\b`），消除 LLM 产生的虚构证券代码 |
| R-LLM-DB-PROCON-09 | HTML 渲染：三段内容以棒棒糖式展开设计，pro 绿色背景、con 红色背景、synthesis 金色背景，视觉区分辩手身份 |
| R-LLM-DB-PROCON-10 | Excel 渲染：pro（绿底）/con（红底）/synthesis（金底）三色块依次输出，标注辩手身份和阶段说明 |
| R-LLM-DB-PROCON-11 | 正反辩论的三段缓存共用 expert_review 的指纹计算策略，持仓品种/份额/成本变化时三段缓存同时失效 |

#### 7.8.3 条件推理

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-DB-CONDITIONAL-01 | 条件推理启用的标志是 Feature Flag `llm_debate_conditional` 为 true |
| R-LLM-DB-CONDITIONAL-02 | 条件推理在智囊团复盘 prompt 中注入预设的市场情景（默认三组：上涨+20%、下跌-20%、震荡±5%），使分析结果情景化 |
| R-LLM-DB-CONDITIONAL-03 | 情景通过 `llm_settings.json` 的 `debate.conditional.scenarios` 配置，每条含 name/change/desc 三个字段，支持用户自定义 |
| R-LLM-DB-CONDITIONAL-04 | 情景注入不改变 LLM 调用次数（仍为单次调用），仅修改 user prompt 内容 |
| R-LLM-DB-CONDITIONAL-05 | 无有效情景配置时（scenarios 为空或全部校验失败），条件推理不生效，回退标准 expert_review prompt |
| R-LLM-DB-CONDITIONAL-06 | 情景内容嵌入智囊团复盘输出中，位于调仓建议段落之前 |

#### 7.8.4 集中度问答

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-LLM-DB-QA-CONCENTRATION-01 | 集中度问答启用的标志是 Feature Flag `llm_debate_qa_concentration` 为 true |
| R-LLM-DB-QA-CONCENTRATION-02 | 系统自动检测持仓集中度，当单品种市值占比 ≥ threshold（默认 0.20）时触发集中度问答块生成 |
| R-LLM-DB-QA-CONCENTRATION-03 | 集中度问答块输出内容：集中度风险的量化评估、与分散化基准的定量对比、针对性的调仓建议 |
| R-LLM-DB-QA-CONCENTRATION-04 | 集中度判定阈值通过 `llm_settings.json` 的 `debate.qa_concentration.threshold` 配置，类型 float，范围 (0, 1)，默认 0.20 |
| R-LLM-DB-QA-CONCENTRATION-05 | 集中度问答块嵌入智囊团深度复盘输出中，位于辩论段落之后、调仓建议之前 |
| R-LLM-DB-QA-CONCENTRATION-06 | 集中度问答不产生独立 LLM API 调用（内容由现有 prompt 模板的 `_build_qa_concentration_block()` 构建：标准模式嵌在 expert_review 的 user prompt 中，辩论模式嵌在综合权衡 synthesis 的 user prompt 中） |

---

## 8. 错误处理与降级需求

### 8.1 数据获取降级

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-ERR-01 | 外部 API 不可用时静默使用备用链路或过期缓存，不得中断报告生成 |
| R-ERR-02 | 缓存文件损坏时自动删除并重新获取，用户无感知 |
| R-ERR-03 | 过期缓存使用天数由配置控制（T2=3 天、T3/T4=14 天） |
| R-ERR-04 | 部分持仓数据缺失时汇总日志列出失败资产的名称和代码 |
| R-ERR-05 | 降级状态在报告中以 ⚠/ℹ 前缀可视化呈现 |
| R-ERR-06 | 降级状态（上次成功时间戳等）应跨会话持久化，存放于 data/state/ 独立目录，与 cache/ 隔离以避免缓存清理误删 |

### 8.2 报告渲染降级

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-ERR-07 | LLM 未配置时不阻塞报告生成，对应章节显示"LLM 未配置" |
| R-ERR-08 | LLM 模块已禁用时完全跳过渲染，不留空位 |
| R-ERR-09 | LLM API 失败时根据失败原因输出差异化占位文本 |
| R-ERR-10 | 基金深度分析模块数据不可用时显示模块级灰色占位文本，不隐藏页签 |
| R-ERR-11 | 新闻 5 源部分失败时在页脚列出失败源清单 |

### 8.3 走势数据降级

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-ERR-12 | 全部持仓历史数据不可用 → 页面显示占位文本 |
| R-ERR-13 | 部分持仓历史数据缺失 → 显示降级警告清单，观测期压缩但不中断 |
| R-ERR-14 | 历史数据获取模式为 off → 显示占位文本，不报错 |
| R-ERR-15 | 持仓快照首次运行无历史数据 → 显示"暂无环比数据" |
| R-ERR-16 | 持仓快照读取异常/损坏 → 日志 WARNING，跳过对比 |

---

# 第二部分：非功能需求

---

## 9. 缓存策略

### 9.1 缓存基本要求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-CCH-01 | 所有外部数据获取结果应缓存到本地磁盘文件，避免重复 HTTP 请求 |
| R-CCH-02 | 缓存文件使用 JSON 格式，超过 100KB 时自动 gzip 压缩 |
| R-CCH-03 | 缓存文件名需防止目录穿越攻击 |
| R-CCH-04 | 缓存写入必须使用原子操作，防止断电/崩溃时产生半写损坏文件 |
| R-CCH-05 | 缓存读取时文件损坏应自动删除并触发重新获取 |

### 9.2 TTL 要求

| 需求标识 | 数据 | 默认 TTL | 盘中特殊 |
|:---------|:-----|:--------:|:--------:|
| R-CCH-06 | 股票/基金最新价 | 24h | 交易时段 30s |
| R-CCH-07 | 市场指数行情 | 24h | 交易时段 30s |
| R-CCH-08 | 财经新闻聚合 | 15 分钟 | — |
| R-CCH-09 | 行业资金流向 | 15 分钟 | — |
| R-CCH-10 | 基金业绩排名 | 24h | — |
| R-CCH-11 | 机构盈利预测 | 24h | — |
| R-CCH-12 | 基金持仓明细 | 7 天 | — |
| R-CCH-13 | 行业分类/概念板块 | 14 天 | — |
| R-CCH-14 | 股票历史分红 | 30 天 | — |
| R-CCH-15 | 业绩比较基准 | 30 天 | — |
| R-CCH-16 | LLM 智囊团深度复盘 | 2h | — |
| R-CCH-17 | LLM 新闻关联分析 | 1h | — |
| R-CCH-18 | LLM 全球政经局势 | 24h | — |
| R-CCH-19 | LLM 持仓体检报告 | 24h | — |
| R-CCH-20 | LLM 穿透深度分析 | 24h | — |
| R-CCH-21 | 基金经理数据 | 24h | — |
| R-CCH-22 | 基金风格扩展数据 | 24h | — |
| R-CCH-23 | 历史 K 线 | 7 天 | — |
| R-CCH-24 | 历史净值 | 30 天 | — |
| R-CCH-25 | 指数历史日线 | 30 天 | — |
| R-CCH-26 | 持仓跟踪数据 | 30 天 | — |
| R-CCH-27 | A 股交易日历 | 14 天 | — |
| R-CCH-28 | LLM 辩论-Pro 阶段 | 24h | — |
| R-CCH-29 | LLM 辩论-Con 阶段 | 24h | — |
| R-CCH-30 | LLM 辩论-Synthesis 阶段 | 24h | — |

### 9.3 缓存分组（手动刷新）

| 需求标识 | 分组 | 包含的缓存类型 | 触发菜单 |
|:---------|:-----|:--------------|:--------:|
| R-CCH-31 | preload（持仓类） | 价格、指数、LLM 四大分析模块、辩论三段缓存（pro/con/synthesis） | 菜单 [2] |
| R-CCH-32 | refresh（基础类） | 基金业绩/持仓/基准/行业/新闻/盈利预测/资金流向/分红/基金经理/重合度/风格扩展 | 菜单 [1] |
| R-CCH-33 | 无分组（受保护） | 持仓跟踪、交易日历、集中度快照、风格快照、历史 K 线、历史净值、指数历史日线 | 仅菜单 [3] 过期清理 |

### 9.4 指纹失效要求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-CCH-34 | 缓存文件名中应内嵌外部输入源的确定性摘要，输入变化时缓存自动失效 |
| R-CCH-35 | 指数行情变化应使盈利预测和资金流向缓存自动失效 |
| R-CCH-36 | 持仓+穿透 A 股代码列表变化应使分红缓存自动失效 |
| R-CCH-37 | 新闻源参数+关键词变化应使新闻缓存自动失效 |
| R-CCH-38 | 持仓品种/份额/成本变化应使 LLM 缓存自动失效（排除行情波动字段） |

---

## 10. 性能需求

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-PERF-01 | 新闻获取应 5 源并行，而非串行拉取 |
| R-PERF-02 | LLM 4+1 模块应并发生成（默认 3 线程），避免串行等待 |
| R-PERF-03 | 取价批量请求应使用异步并发，避免逐只串行 |
| R-PERF-04 | 运行时配置读取应有内存缓存，按 mtime 自动检查更新 |
| R-PERF-05 | LLM 提示词应控制 token 消耗（精简模式 ≤300 字） |
| R-PERF-06 | 风格与因子分析中同一品种的 API 数据应在会话内跨基金复用 |
| R-PERF-07 | 历史数据获取应使用增量模式（缓存底座 + 增量新数据），避免全量重取 |
| R-PERF-08 | 历史走势获取应阶段超时保护（超时后已完成部分保留，未完成部分以占位处理） |
| R-PERF-09 | 每次报告生成应自动记录各阶段耗时（行情获取/数据准备/HTML生成/Excel生成/LLM+新闻等），持久化到 `data/state/perf_history.jsonl` 用于跨版本性能趋势追踪 |
| R-PERF-10 | 每次报告生成应在后台自动执行数据源健康检查（HTTP 连通性+延迟），结果存入 `data/state/datasource_health.jsonl` 并注入 DegradationTracker 供数据源可用性矩阵展示 |

---

## 11. 配置需求

### 11.1 config.json

| 字段 | 类型 | 默认值 | 可 TUI 修改 | 说明 |
|:-----|:----:|:------:|:----------:|:-----|
| `holdings_dir` | str | `data/holdings` | ✅ C | 持仓 xlsx 目录 |
| `holdings_filename` | str | `个人投资持仓信息.xlsx` | ✅ F | 持仓文件名 |
| `output_dir` | str | `reports` | ✅ O | 报告输出根目录 |
| `news_top_count` | int | 300 | — | 新闻关联输出 TOP N |
| `news_sources` | dict | sina/eastmoney/wallstreetcn/akshare 开启，cls 默认关闭 | — | 各新闻源启停 |
| `preferred_provider` | dict | 空 | — | 各数据类型的首选 Provider |
| `user_fund_benchmarks` | dict | 空 | — | 自定义基金业绩基准 |
| `risk_free_rate` | float/null | null | — | 无风险利率手动配置（null=自动从国债收益率获取，填小数如0.0174或百分比如1.74） |
| `report_section_order` | dict | 空 | — | 报告模块序号自定义 |
| `anonymization.mode` | str | `"off"` | ✅ A | 匿名化模式：`off` / `code_display` / `full_anonymous` / `summary` |
| `enable_fund_deep_analysis` | bool | true | ✅ P | 基金深度分析启停 |
| `enable_news` | bool | true | ✅ P | 市场新闻启停 |
| `enable_history` | bool | true | ✅ P | 历史走势启停 |
| `enable_portfolio_evolution` | bool | true | ✅ P | 组合演进启停 |
| `enable_action` | bool | true | ✅ P | 行动建议启停（默认开；再平衡信号/交易纪律/调仓建议/收益归因） |
| `market_hour_aware` | list | `["price","index"]` | — | 交易时段短 TTL 的数据类型 |
| `market_hour_ttl` | int | 30 | — | 交易时段缓存有效期（秒） |
| `market_hours` | dict | `{start:"09:30",end:"15:00",official_source:true}` | — | 交易时段配置（official_source=true 以官方行情源判定开市/收盘；false 不使用官方源，返回 None 走 fallback） |
| `degradation` | dict | T2/T3/T4 默认 | — | T2（基金业绩/行业分类等, stale_days=3）/ T3（资金流向/基金持仓等, stale_days=14）/ T4（穿透数据, stale_days=14），每层含 unreachable_threshold / empty_data_threshold / stale_days 三参数 |
| `history.fetch_mode` | str | "auto" | — | 历史走势获取模式（off/prompt/auto） |
| `history.snapshot_retention_days` | int | 60 | — | 快照保留天数 |
| `history.snapshot_max_count` | int | 365 | — | 快照最大数量 |
| `history.coverage_threshold` | float | 0.8 | — | 有效区间覆盖阈值 |
| `history.benchmark_indices` | dict | {"sh000300":"沪深300"} | — | 基准指数配置，组合历史走势对比 |
| `history.lookback_days` | int | 90 | — | 历史走势取数窗口（K 线条数/交易日），需 ≥ 60（MIN_SPAN）才能计算回撤分析 |
| `features.json` 的 8 项报告开关 | bool | `data_quality` 与 `market_temperature` 默认开，其余 6 项默认关 | — | **报告章节与增强开关**（登记于功能开关注册表 `GROUP_REPORT`，存 `data/config/features.json`，菜单 `[S]` 报告块）：`data_quality`（数据质量仪表盘）/ `market_temperature`（市场温度，无数据时该行静默省略）/ `industry_beta`（行业 Beta 子表）/ `candidate_compare`（候选基金比较）/ `cost_lots`（成本流水）/ `valuation_percentile`（估值分位）/ `financial_report_digest`（持仓个股财报摘要）/ `financial_indicator`（财务指标） |
| `holdings_start_date` | str | `""` | — | 组合建仓日期（YYYY-MM-DD，可选）。持仓 Excel 未录入交易/分红流水时，成本流水子模块按「建仓日一次性买入」近似年化（`build_approximate_fund_flow_data`）；空=不计算近似年化，仅成本分档近似（每份成本 vs 市价） |
| `comparison_indices` | dict | {"sh000300":"沪深300","sh000905":"中证500","sh000012":"中证全债"} | — | 竞争语境对比指数池，支持多指数对比 |
| `rebalance.threshold` | float | 0.15 | — | 单品种权重超限阈值（15%），超限触发再平衡建议 |
| `rebalance.deviation_threshold` | float | 0.05 | — | 大类/品种配置偏离阈值（5%），权益/固收偏离超限时触发调整建议 |
| `rebalance.profile` | str | "moderate" | — | 预设阈值集：conservative（保守 10%/3%）/ moderate（稳健 15%/5%）/ aggressive（进取 25%/8%）/ custom（自定义） |
| `rebalance.silence_days` | int | 30 | — | 再平衡信号静默期天数，同品种触发 N 天内不重复告警 |
| `rebalance.target_allocation` | dict | {} | — | 目标配置 Schema（空=不启用），格式 `{"equity":{"min":30,"max":70,"target":50},"bond":{...}}` |
| `rebalance.equity_fixed_income` | dict | {} | — | 权益/固收超大类目标配置（空=不启用），格式 `{"equity":{"min":30,"max":70}}` |
| `discipline.take_profit_pct` | float | 20.0 | — | 止盈线（%）：单品种收益率 ≥ 此值 → 建议部分止盈 |
| `discipline.stop_loss_pct` | float | -15.0 | — | 止损线（%）：单品种收益率 ≤ 此值 → 建议止损/减仓 |
| `discipline.drawdown_pct` | float | -10.0 | — | 回撤线（%）：组合相对历史峰值回撤 ≥ 此绝对值 → 建议控回撤 |
| `discipline.silence_days` | int | 30 | — | 交易纪律信号静默期天数，同品种触发 N 天内不重复告警 |
| `redemption_limits` | dict | {} | — | 场外基金单日赎回上限，格式 `{基金代码: 金额}`。配置后计算场外品种全量赎回所需天数 |
| `cache_ttl` | dict | 默认 TTL | — | 各缓存类型 TTL 覆盖 |
| `llm_key_file` | str | `data/config/llm_key.json` | — | LLM 密钥文件路径（敏感凭据，纳入 .gitignore） |
| `llm_providers_file` | str | `data/config/llm_providers.json` | — | LLM Provider 多链配置文件路径 |
| `llm_settings_file` | str | `data/config/llm_settings.json` | — | LLM 参数文件路径 |

> **config.json 跨机器同步**：config.json 受 git 跟踪，是可跨机器同步的配置文件，仅存放可共享的业务配置。机器个性化运行时标志（首次运行引导 `_startup_wizard_shown`、隐私提示已读 `_privacy_notice_shown`）**不存放于 config.json**，而是存于 `data/state/local_state.json`（git 忽略、仅本机可见），避免各机器写入差异导致 config.json 难以同步。这两个键只在 `data/state/local_state.json` 中读写，不做任何 config.json 旧键迁移。

### 11.2 llm_key.json（敏感凭据文件，建议纳入 .gitignore）

多键凭据字典，支持按 `credentials_ref` 名称引用。

| 键 | 类型 | 必填 | 说明 |
|:---|:----:|:----:|:-----|
| `{ref_name}` | dict | ✅ | 凭据块，任意合法键名（如 `deepseek-main`） |
| `{ref_name}.api_key` | str | ✅ | API 密钥 |
| `{ref_name}.model` | str | ✅ | 模型名 |
| `{ref_name}.endpoint` | str | ✅ | API 端点 URL |
| `{ref_name}.provider` | str | — | Provider 类型（`claude` / `openai` / `gemini`），仅在覆盖 `llm_providers.json` 时使用 |

**简化格式**（兼容单键凭据格式，无需 `credentials_ref`）：若文件顶层仅含 `{"api_key": "...", "model": "..."}` 等单键凭据字段，自动包裹为 `{"_default": {...}}` 处理。

### 11.3 llm_settings.json（非敏感参数）

**全局配置**：

| 键 | 类型 | 默认值 | 说明 |
|:---|:----:|:------:|:-----|
| `max_retries` | int | 2 | 429/503 最大重试次数 |
| `llm_max_concurrency` | int | 3 | LLM 并发生成最大线程数 |
| `llm_max_thinking_concurrency` | int | 1 | Extended Thinking 模块并发信号量上限（防止多 thinking 模块并发偶发空响应） |
| `enabled_llm` | dict | 全局开启 | 各模块独立启停 |
| `pricing` | dict | — | 模型定价表覆盖 + 货币设置 |

**模块级配置**（`{key}` = global_macro / expert_review / health_check / penetration_deep / news_correlation）：

| 键 | 类型 | 说明 |
|:---|:----:|:-----|
| `system_prompt_{key}` | str/null | 自定义 System Prompt |
| `model_{key}` | str/null | 模块级模型覆盖 |
| `temperature_{key}` | float | 生成温度（0.1~0.8） |
| `max_tokens_{key}` | int | 最大输出 Token |
| `timeout_{key}` | int | API 超时秒数 |
| `cache_enabled_{key}` | bool | 是否缓存 LLM 结果 |
| `output_brief_{key}` | bool | 精简模式（≤300 字、通常 200 字左右，news_correlation 不支持） |
| `thinking_enabled_{key}` | bool | Extended Thinking 开关 |
| `thinking_budget_{key}` | int | Claude Thinking 预算 |
| `reasoning_effort_{key}` | str | DeepSeek 推理深度 |

**辩论模式配置**（键名前缀 `debate.`，存放于 llm_settings.json 的 `debate` 配置段）：

| 键 | 类型 | 默认值 | 说明 |
|:---|:----:|:------:|:-----|
| `debate.procon.per_call_max_tokens` | int/null | 18432 | 正反辩论每阶段 max_tokens 覆盖（null=按代码兜底 18432；2026-09-16 由 8192 两轮各上调 50%：先修 pro 段被 8192 截断，再为思考型模型留 thinking 预算余量），经 `max_tokens_override` 优先于模块级 `max_tokens_expert_review` |
| `debate.procon.synthesis_model` | str/null | null | 正反辩论综合阶段模型覆盖（null=使用 pro/con 相同的模型） |
| `debate.procon.synthesis_temperature` | float | 0.5 | 正反辩论综合阶段 temperature（低于常规以保持客观，范围 [0.0, 2.0]） |
| `debate.conditional.scenarios` | list[dict] | 上涨/下跌/震荡 三组 | 条件推理预设情景列表，每条含 `name`（情景名）/ `change`（涨跌幅）/ `desc`（描述）三个必填字段 |
| `debate.qa_concentration.threshold` | float | 0.20 | 集中度问答触发阈值（单品种占比 ≥ 此值时触发），范围 (0, 1) |
| `debate.max_total_tokens_per_report` | int | 72000 | 单次报告辩论模式总 token 预算上限（超出后跳过 debate 回退标准模式） |
| `debate.per_call_timeout_override` | int | 90 | 辩论模式单次 API 调用超时覆盖秒数 |

### 11.4 llm_providers.json（Provider 多链配置）

定义 LLM Provider 的切换策略和备选 Provider 列表，不包含敏感凭据（通过 credentials_ref 引用 llm_key.json）。

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|:-----|:----:|:----:|:------:|:-----|
| `strategy` | str | — | `"priority"` | 切换策略：priority / weighted / cost_first / fallback_only。每条 Provider 可额外设置 `proxy_preferred: true`，有代理环境时自动前置 |
| `preferred_providers` | dict | — | `{}` | 模块级首选：`{"news": "provider_name"}`，指定模块强制路由到特定 Provider |
| `providers` | list[dict] | ✅ | — | Provider 条目数组（按优先级顺序，至少 1 条） |

**Provider 条目字段**：

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|:-----|:----:|:----:|:------:|:-----|
| `name` | str | ✅ | — | 唯一标识名称，用于引用和日志 |
| `provider` | str | ✅ | — | Provider 类型：`claude` / `openai` / `gemini` |
| `credentials_ref` | str | ✅ | — | 引用 `llm_key.json` 中的凭据块名——**凭据的唯一合法来源，必填**；缺失、非字符串或纯空白时该条目校验不通过并被跳过 |
| `api_key` | str | — | — | **禁止出现在本文件**（凭据分离：本文件受版本控制）。出现非空值时该条目校验不通过并被跳过，提示将密钥写入 `llm_key.json` 的凭据块后以 `credentials_ref` 引用 |
| `model` | str | — | — | 可选的非敏感路由覆盖；与凭据块同名键同名时**本条目的值优先**，缺省（或空白）时取凭据块的值 |
| `endpoint` | str | — | 各 Provider 默认值 | 端点 URL 覆盖 |
| `priority` | int | — | 99 | 优先级（数值越小优先级越高），priority 策略使用 |
| `weight` | int | — | 1 | 权重值，weighted 策略使用 |
| `timeout` | float | — | 60.0 | API 超时秒数 |
| `proxy_preferred` | bool | — | false | 是否优先使用 proxy（per-provider 后处理标记，有代理环境时自动前置） |

**示例**：
```json
{
  "strategy": "priority",
  "preferred_providers": {"news": "deepseek-main"},
  "providers": [
    {"name": "deepseek-main", "provider": "claude", "credentials_ref": "deepseek-main", "priority": 10, "timeout": 120},
    {"name": "gemini-fallback", "provider": "gemini", "credentials_ref": "gemini-fb", "priority": 20, "timeout": 60}
  ]
}
```

### 11.5 features.json（功能开关注册表）

独立配置文件，提供 29 项功能开关的运行时覆写。不配置时全部使用代码内置默认值。开关分**实验组**（4 项，默认关，面板以 ⚗ 标识）、**常规组**（16 项，默认开）与**报告章节与增强组**（8 项，多数默认关）三块，分组表达「生命周期的当前状态」而非优先级——**转正**即把声明从实验组改到常规组、默认值改 `true`，面板可见性随分组自动延续。两组在 TUI 菜单 `[S]` / Web 配置面板 / CLI（`--experiment` 实验组简写、`--feature NAME=VALUE` 全域双向）中**同样可切换**；唯一登记点是 `config/features.py::feature_switch_registry`（显示名/说明/分组/默认值/产物影响五字段），渠道层不得另写清单。仅收录「有消费者」的开关——LLM 模块启停与基金深度分析（`llm_settings.json` 的 `enabled_llm`）、新闻源（`config.json` 的 `news_sources`）、历史走势与回撤（`config.json` 的 `enable_history`）、匿名化模式（`config.json` 的 `anonymization.mode`）各有归属配置，不在此文件。

| 开关名 | 类型 | 默认值 | 说明 |
|:-------|:----:|:------:|:-----|
| `llm_debate_procon` / `llm_debate_qa_concentration` | bool | false（默认关闭） | 辩论模式两增强通路独立启停：正反辩论/集中度问答 |
| `llm_debate_conditional` | bool | true（默认开启） | 辩论-条件推理启停：在既有分析调用内追加上涨/下跌/震荡情景段（**不增加调用次数**）。**默认开启**——情景段与既有输出在同一次调用内产出 |
| `decision_reflection` | bool | false（默认关闭） | 决策跨期反思闭环启停：登记决策 → 真实行情结算命中率 → 教训回灌专家复盘提示词 |
| `signal_pre_digest` | bool | true（默认开启） | 信号预消化启停：市场温度/估值分位/尾部风险/**持仓基本面**/**叙事与数字背离**五路预消化为带方向标注的信号行（`信号：… 看多/看空/中性/风险高/中/低/需交叉核实`）注入专家复盘与持仓体检提示词（后两路需 DataSinking 数据底座就绪，未就绪自动缺席）。**默认开启**——读侧注入既有信号，不额外调用 LLM、不写盘；无可用信号时静默跳过、缓存键逐字节不变 |
| `module_quality_gate` | bool | true（默认开启） | 模块级质量分级启停：对 4 个 LLM 模块输出按完整性/篇幅评 A~F，低评级中「内容在但存在缺陷」者随内容头部注入 `【内容质量提示】` 横幅（只标注、不阻断、不重试、不写回缓存）。**默认开启**——纯只读分级，A/B 级健康输出零噪音 |
| `decision_header_parse` | bool | true（默认开启） | 决策头结构化启停：专家复盘提示词追加一行受控 JSON 决策头（`决策头：{"decisions":[…]}`），抽取侧优先读结构化头、失败回落确定性表格解析（决策词归一，防写反方向）；关闭时提示词逐字节不变。**默认开启**——两路抽取口径同源，关闭只是去掉一层机器可读保障 |
| `signal_ledger` | bool | false（默认关闭） | 确定性数值信号沉淀启停：把市场温度/估值分位/尾部风险/风格因子/再平衡超限五类确定性评级沉淀为 `data/state/signal_ledger.jsonl` 账本，每条附实时-非实时来源标签；统计与注入提示词的摘要默认只算实时记录，防非实时记录冒充真实战绩 |
| `metrics_sharpe` / `metrics_calmar` / `metrics_hhi` / `metrics_winrate` / `metrics_turnover` / `metrics_risk_contribution` / `metrics_beta` | bool | true | 量化指标独立启停（夏普/卡玛/HHI/胜率/换手率/风险贡献/Beta） |
| `enable_interactive_charts` | bool | true | 报告 HTML 交互图表（Chart.js）；关闭时回退基础 Canvas 图表 |
| `doctor_check` | bool | true（默认开启） | 系统自检功能上屏：开启时 TUI 菜单显示 `[D]` 系统自检项、Web 运行状态区渲染「系统自检」卡片（`GET /api/doctor`）；置 false 则两处入口一并隐藏。**仅约束 TUI/Web 两个日常入口**——`doctor` CLI 子命令不受本开关约束（配置损坏正是它要诊断的场景，被开关拦住会形成死锁）。属**常规组**开关（TUI 菜单 `[S]` / Web 配置面板的「常规开关」块），只影响入口显隐、不进报告产物自述 |
| `datasource_adapter` | bool | true（默认开启） | 数据源适配契约启停：行情域三源改由三段式适配器（参数转译→抓取→映射到标准字段）+ 声明式 alias 归一获取，与既有转换函数逐源等价（仅东方财富多出 `market_cap`/`pe` 两个 `None` 键）。**默认开启**——内部接缝，两种取值下报告数值不变；置 false 时链路走既有转换函数、行为逐字节不变（回退杠杆）。为接入新数据源/新字段预备 |
| `datasource_credential_ready` | bool | true（默认开启） | 数据源凭据就绪指引启停：接入需 key 的源时补一行声明，链路即**主动跳过**缺凭据的源并给出可读指引（不进熔断计数）、`check-sources` 出 `⏭️` 跳过态与就绪摘要、`doctor` 出「数据源凭据」组。**当前全部数据源免费、声明表为空**，开启后可见的就是「均无需凭据（免费源）」。凭据只从环境变量读，**值永不落日志与报告**。**默认开启**——声明表为空时对报告产物零影响，且只读诊断不耗 LLM、不写盘 |
| `feeder_penetration` | bool | true（默认开启） | 联接基金穿透启停：ETF 联接基金按其**目标 ETF** 的持仓与报告期穿透底层资产（联接基金本身不持有股票，其季报股票表按构造为空）；目标 ETF 由基金主页面锚点动态解析，报告标注「穿透自目标 ETF `XXXXXX`（未折算持有比例）」。置 false 即维持「联接基金无底层资产」（用户报障前的行为） |

用法：在 `features.json` 中仅列出需覆写的开关，未列出的保持默认值。
```json
{
  "enable_interactive_charts": true,
  "metrics_hhi": false
}
```

无消费者键（陈旧开关或拼写错误）不驱动任何行为：`load_feature_overrides()` 仍加载其值，但合并成一条 WARNING 列出键名，避免用户以为配置已生效。

---

## 12. 约束与假设

| 需求标识 | 需求描述 |
|:---------|:---------|
| R-CON-01 | LLM key 未配置时整流程不阻塞，对应页签显示占位，不报错退出 |
| R-CON-02 | config.json 损坏时回退全默认值，不崩溃 |
| R-CON-03 | 持仓文件格式异常时跳过异常行继续解析，不中断 |
| R-CON-04 | 报告输出目录无写入权限时提示错误后退出，不静默失败 |
| R-CON-05 | 空持仓时暂停生成，直接返回 |
| R-CON-06 | 数据源 API 变更/不可用时，各模块独立降级，互不干扰 |
| R-CON-07 | 网络断开时无法使用备用链路的请求降级到过期缓存，无缓存则跳过 |
| R-CON-08 | 所有降级/失败信息必须在报告中可视化展示，不得静默吞没 |
